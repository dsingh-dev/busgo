import {
  clsx_default
} from "./chunk-U7P2NEEE.js";
import {
  require_react_dom
} from "./chunk-UZOKQUDP.js";
import {
  require_jsx_runtime
} from "./chunk-PXMHN2KN.js";
import {
  require_react
} from "./chunk-OIM7OLJW.js";
import {
  __commonJS,
  __toESM
} from "./chunk-RDKGUBC5.js";

// node_modules/warning/warning.js
var require_warning = __commonJS({
  "node_modules/warning/warning.js"(exports, module) {
    "use strict";
    var __DEV__ = true;
    var warning2 = function() {
    };
    if (__DEV__) {
      printWarning = function printWarning2(format, args) {
        var len = arguments.length;
        args = new Array(len > 1 ? len - 1 : 0);
        for (var key = 1; key < len; key++) {
          args[key - 1] = arguments[key];
        }
        var argIndex = 0;
        var message = "Warning: " + format.replace(/%s/g, function() {
          return args[argIndex++];
        });
        if (typeof console !== "undefined") {
          console.error(message);
        }
        try {
          throw new Error(message);
        } catch (x) {
        }
      };
      warning2 = function(condition, format, args) {
        var len = arguments.length;
        args = new Array(len > 2 ? len - 2 : 0);
        for (var key = 2; key < len; key++) {
          args[key - 2] = arguments[key];
        }
        if (format === void 0) {
          throw new Error(
            "`warning(condition, format, ...args)` requires a warning message argument"
          );
        }
        if (!condition) {
          printWarning.apply(null, [format].concat(args));
        }
      };
    }
    var printWarning;
    module.exports = warning2;
  }
});

// node_modules/react-datetime-picker/dist/DateTimePicker.js
var import_jsx_runtime40 = __toESM(require_jsx_runtime(), 1);
var import_react10 = __toESM(require_react(), 1);
var import_react_dom = __toESM(require_react_dom(), 1);

// node_modules/make-event-props/dist/index.js
var clipboardEvents = ["onCopy", "onCut", "onPaste"];
var compositionEvents = [
  "onCompositionEnd",
  "onCompositionStart",
  "onCompositionUpdate"
];
var focusEvents = ["onFocus", "onBlur"];
var formEvents = ["onInput", "onInvalid", "onReset", "onSubmit"];
var imageEvents = ["onLoad", "onError"];
var keyboardEvents = ["onKeyDown", "onKeyPress", "onKeyUp"];
var mediaEvents = [
  "onAbort",
  "onCanPlay",
  "onCanPlayThrough",
  "onDurationChange",
  "onEmptied",
  "onEncrypted",
  "onEnded",
  "onError",
  "onLoadedData",
  "onLoadedMetadata",
  "onLoadStart",
  "onPause",
  "onPlay",
  "onPlaying",
  "onProgress",
  "onRateChange",
  "onSeeked",
  "onSeeking",
  "onStalled",
  "onSuspend",
  "onTimeUpdate",
  "onVolumeChange",
  "onWaiting"
];
var mouseEvents = [
  "onClick",
  "onContextMenu",
  "onDoubleClick",
  "onMouseDown",
  "onMouseEnter",
  "onMouseLeave",
  "onMouseMove",
  "onMouseOut",
  "onMouseOver",
  "onMouseUp"
];
var dragEvents = [
  "onDrag",
  "onDragEnd",
  "onDragEnter",
  "onDragExit",
  "onDragLeave",
  "onDragOver",
  "onDragStart",
  "onDrop"
];
var selectionEvents = ["onSelect"];
var touchEvents = ["onTouchCancel", "onTouchEnd", "onTouchMove", "onTouchStart"];
var pointerEvents = [
  "onPointerDown",
  "onPointerMove",
  "onPointerUp",
  "onPointerCancel",
  "onGotPointerCapture",
  "onLostPointerCapture",
  "onPointerEnter",
  "onPointerLeave",
  "onPointerOver",
  "onPointerOut"
];
var uiEvents = ["onScroll"];
var wheelEvents = ["onWheel"];
var animationEvents = [
  "onAnimationStart",
  "onAnimationEnd",
  "onAnimationIteration"
];
var transitionEvents = ["onTransitionEnd"];
var otherEvents = ["onToggle"];
var changeEvents = ["onChange"];
var allEvents = [
  ...clipboardEvents,
  ...compositionEvents,
  ...focusEvents,
  ...formEvents,
  ...imageEvents,
  ...keyboardEvents,
  ...mediaEvents,
  ...mouseEvents,
  ...dragEvents,
  ...selectionEvents,
  ...touchEvents,
  ...pointerEvents,
  ...uiEvents,
  ...wheelEvents,
  ...animationEvents,
  ...transitionEvents,
  ...changeEvents,
  ...otherEvents
];
function makeEventProps(props, getArgs) {
  const eventProps = {};
  for (const eventName of allEvents) {
    const eventHandler = props[eventName];
    if (!eventHandler) {
      continue;
    }
    if (getArgs) {
      eventProps[eventName] = (event) => eventHandler(event, getArgs(eventName));
    } else {
      eventProps[eventName] = eventHandler;
    }
  }
  return eventProps;
}

// node_modules/react-calendar/dist/Calendar/Navigation.js
var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);

// node_modules/mimic-function/index.js
var copyProperty = (to, from, property, ignoreNonConfigurable) => {
  if (property === "length" || property === "prototype") {
    return;
  }
  if (property === "arguments" || property === "caller") {
    return;
  }
  const toDescriptor = Object.getOwnPropertyDescriptor(to, property);
  const fromDescriptor = Object.getOwnPropertyDescriptor(from, property);
  if (!canCopyProperty(toDescriptor, fromDescriptor) && ignoreNonConfigurable) {
    return;
  }
  Object.defineProperty(to, property, fromDescriptor);
};
var canCopyProperty = function(toDescriptor, fromDescriptor) {
  return toDescriptor === void 0 || toDescriptor.configurable || toDescriptor.writable === fromDescriptor.writable && toDescriptor.enumerable === fromDescriptor.enumerable && toDescriptor.configurable === fromDescriptor.configurable && (toDescriptor.writable || toDescriptor.value === fromDescriptor.value);
};
var changePrototype = (to, from) => {
  const fromPrototype = Object.getPrototypeOf(from);
  if (fromPrototype === Object.getPrototypeOf(to)) {
    return;
  }
  Object.setPrototypeOf(to, fromPrototype);
};
var wrappedToString = (withName, fromBody) => `/* Wrapped ${withName}*/
${fromBody}`;
var toStringDescriptor = Object.getOwnPropertyDescriptor(Function.prototype, "toString");
var toStringName = Object.getOwnPropertyDescriptor(Function.prototype.toString, "name");
var changeToString = (to, from, name) => {
  const withName = name === "" ? "" : `with ${name.trim()}() `;
  const newToString = wrappedToString.bind(null, withName, from.toString());
  Object.defineProperty(newToString, "name", toStringName);
  const { writable, enumerable, configurable } = toStringDescriptor;
  Object.defineProperty(to, "toString", { value: newToString, writable, enumerable, configurable });
};
function mimicFunction(to, from, { ignoreNonConfigurable = false } = {}) {
  const { name } = to;
  for (const property of Reflect.ownKeys(from)) {
    copyProperty(to, from, property, ignoreNonConfigurable);
  }
  changePrototype(to, from);
  changeToString(to, from, name);
  return to;
}

// node_modules/memoize/distribution/index.js
var maxTimeoutValue = 2147483647;
var cacheStore = /* @__PURE__ */ new WeakMap();
var cacheTimerStore = /* @__PURE__ */ new WeakMap();
var cacheKeyStore = /* @__PURE__ */ new WeakMap();
function getValidCacheItem(cache, key) {
  const item = cache.get(key);
  if (!item) {
    return void 0;
  }
  if (item.maxAge <= Date.now()) {
    cache.delete(key);
    return void 0;
  }
  return item;
}
function memoize(function_, { cacheKey, cache = /* @__PURE__ */ new Map(), maxAge } = {}) {
  if (maxAge === 0) {
    return function_;
  }
  if (typeof maxAge === "number" && Number.isFinite(maxAge)) {
    if (maxAge > maxTimeoutValue) {
      throw new TypeError(`The \`maxAge\` option cannot exceed ${maxTimeoutValue}.`);
    }
    if (maxAge < 0) {
      throw new TypeError("The `maxAge` option should not be a negative number.");
    }
  }
  const memoized = function(...arguments_) {
    var _a;
    const key = cacheKey ? cacheKey(arguments_) : arguments_[0];
    const cacheItem = getValidCacheItem(cache, key);
    if (cacheItem) {
      return cacheItem.data;
    }
    const result = function_.apply(this, arguments_);
    const computedMaxAge = typeof maxAge === "function" ? maxAge(...arguments_) : maxAge;
    if (computedMaxAge !== void 0 && computedMaxAge !== Number.POSITIVE_INFINITY) {
      if (!Number.isFinite(computedMaxAge)) {
        throw new TypeError("The `maxAge` function must return a finite number, `0`, or `Infinity`.");
      }
      if (computedMaxAge <= 0) {
        return result;
      }
      if (computedMaxAge > maxTimeoutValue) {
        throw new TypeError(`The \`maxAge\` function result cannot exceed ${maxTimeoutValue}.`);
      }
    }
    cache.set(key, {
      data: result,
      maxAge: computedMaxAge === void 0 || computedMaxAge === Number.POSITIVE_INFINITY ? Number.POSITIVE_INFINITY : Date.now() + computedMaxAge
    });
    if (computedMaxAge !== void 0 && computedMaxAge !== Number.POSITIVE_INFINITY) {
      const timer = setTimeout(() => {
        var _a2;
        cache.delete(key);
        (_a2 = cacheTimerStore.get(memoized)) == null ? void 0 : _a2.delete(timer);
      }, computedMaxAge);
      (_a = timer.unref) == null ? void 0 : _a.call(timer);
      const timers = cacheTimerStore.get(memoized) ?? /* @__PURE__ */ new Set();
      timers.add(timer);
      cacheTimerStore.set(memoized, timers);
    }
    return result;
  };
  mimicFunction(memoized, function_, {
    ignoreNonConfigurable: true
  });
  cacheStore.set(memoized, cache);
  cacheKeyStore.set(memoized, cacheKey ?? ((arguments_) => arguments_[0]));
  return memoized;
}

// node_modules/get-user-locale/dist/index.js
function isString(el) {
  return typeof el === "string";
}
function isUnique(el, index, arr) {
  return arr.indexOf(el) === index;
}
function isAllLowerCase(el) {
  return el.toLowerCase() === el;
}
function fixCommas(el) {
  return el.indexOf(",") === -1 ? el : el.split(",");
}
function normalizeLocale(locale) {
  if (!locale) {
    return locale;
  }
  if (locale === "C" || locale === "posix" || locale === "POSIX") {
    return "en-US";
  }
  if (locale.indexOf(".") !== -1) {
    var _a = locale.split(".")[0], actualLocale = _a === void 0 ? "" : _a;
    return normalizeLocale(actualLocale);
  }
  if (locale.indexOf("@") !== -1) {
    var _b = locale.split("@")[0], actualLocale = _b === void 0 ? "" : _b;
    return normalizeLocale(actualLocale);
  }
  if (locale.indexOf("-") === -1 || !isAllLowerCase(locale)) {
    return locale;
  }
  var _c = locale.split("-"), splitEl1 = _c[0], _d = _c[1], splitEl2 = _d === void 0 ? "" : _d;
  return "".concat(splitEl1, "-").concat(splitEl2.toUpperCase());
}
function getUserLocalesInternal(_a) {
  var _b = _a === void 0 ? {} : _a, _c = _b.useFallbackLocale, useFallbackLocale = _c === void 0 ? true : _c, _d = _b.fallbackLocale, fallbackLocale = _d === void 0 ? "en-US" : _d;
  var languageList = [];
  if (typeof navigator !== "undefined") {
    var rawLanguages = navigator.languages || [];
    var languages = [];
    for (var _i = 0, rawLanguages_1 = rawLanguages; _i < rawLanguages_1.length; _i++) {
      var rawLanguagesItem = rawLanguages_1[_i];
      languages = languages.concat(fixCommas(rawLanguagesItem));
    }
    var rawLanguage = navigator.language;
    var language = rawLanguage ? fixCommas(rawLanguage) : rawLanguage;
    languageList = languageList.concat(languages, language);
  }
  if (useFallbackLocale) {
    languageList.push(fallbackLocale);
  }
  return languageList.filter(isString).map(normalizeLocale).filter(isUnique);
}
var getUserLocales = memoize(getUserLocalesInternal, { cacheKey: JSON.stringify });
function getUserLocaleInternal(options) {
  return getUserLocales(options)[0] || null;
}
var getUserLocale = memoize(getUserLocaleInternal, { cacheKey: JSON.stringify });
var dist_default = getUserLocale;

// node_modules/react-calendar/dist/shared/dateFormatter.js
var formatterCache = /* @__PURE__ */ new Map();
function getFormatter(options) {
  return function formatter(locale, date) {
    const localeWithDefault = locale || dist_default();
    if (!formatterCache.has(localeWithDefault)) {
      formatterCache.set(localeWithDefault, /* @__PURE__ */ new Map());
    }
    const formatterCacheLocale = formatterCache.get(localeWithDefault);
    if (!formatterCacheLocale.has(options)) {
      const formatter2 = new Intl.DateTimeFormat(localeWithDefault || void 0, options);
      formatterCacheLocale.set(options, formatter2.format.bind(formatter2));
    }
    return formatterCacheLocale.get(options)(date);
  };
}
function toSafeHour(date) {
  const safeDate = new Date(date);
  return new Date(safeDate.setHours(12));
}
function getSafeFormatter(options) {
  return (locale, date) => getFormatter(options)(locale, toSafeHour(date));
}
var formatDateOptions = {
  day: "numeric",
  month: "numeric",
  year: "numeric"
};
var formatDayOptions = { day: "numeric" };
var formatLongDateOptions = {
  day: "numeric",
  month: "long",
  year: "numeric"
};
var formatMonthOptions = { month: "long" };
var formatMonthYearOptions = {
  month: "long",
  year: "numeric"
};
var formatShortWeekdayOptions = { weekday: "short" };
var formatWeekdayOptions = { weekday: "long" };
var formatYearOptions = { year: "numeric" };
var formatDate = getSafeFormatter(formatDateOptions);
var formatDay = getSafeFormatter(formatDayOptions);
var formatLongDate = getSafeFormatter(formatLongDateOptions);
var formatMonth = getSafeFormatter(formatMonthOptions);
var formatMonthYear = getSafeFormatter(formatMonthYearOptions);
var formatShortWeekday = getSafeFormatter(formatShortWeekdayOptions);
var formatWeekday = getSafeFormatter(formatWeekdayOptions);
var formatYear = getSafeFormatter(formatYearOptions);

// node_modules/@wojtekmaj/date-utils/dist/index.js
function makeGetEdgeOfNeighbor(getPeriod, getEdgeOfPeriod, defaultOffset) {
  return function makeGetEdgeOfNeighborInternal(date, offset = defaultOffset) {
    const previousPeriod = getPeriod(date) + offset;
    return getEdgeOfPeriod(previousPeriod);
  };
}
function makeGetEnd(getBeginOfNextPeriod) {
  return function makeGetEndInternal(date) {
    return new Date(getBeginOfNextPeriod(date).getTime() - 1);
  };
}
function makeGetRange(getStart, getEnd2) {
  return function makeGetRangeInternal(date) {
    return [getStart(date), getEnd2(date)];
  };
}
function getYear(date) {
  if (date instanceof Date) {
    return date.getFullYear();
  }
  if (typeof date === "number") {
    return date;
  }
  const year = Number.parseInt(date, 10);
  if (typeof date === "string" && !Number.isNaN(year)) {
    return year;
  }
  throw new Error(`Failed to get year from date: ${date}.`);
}
function getMonth(date) {
  if (date instanceof Date) {
    return date.getMonth();
  }
  throw new Error(`Failed to get month from date: ${date}.`);
}
function getMonthHuman(date) {
  if (date instanceof Date) {
    return date.getMonth() + 1;
  }
  throw new Error(`Failed to get human-readable month from date: ${date}.`);
}
function getDate(date) {
  if (date instanceof Date) {
    return date.getDate();
  }
  throw new Error(`Failed to get year from date: ${date}.`);
}
function getHours(date) {
  if (date instanceof Date) {
    return date.getHours();
  }
  if (typeof date === "string") {
    const datePieces = date.split(":");
    if (datePieces.length >= 2) {
      const hoursString = datePieces[0];
      if (hoursString) {
        const hours = Number.parseInt(hoursString, 10);
        if (!Number.isNaN(hours)) {
          return hours;
        }
      }
    }
  }
  throw new Error(`Failed to get hours from date: ${date}.`);
}
function getMinutes(date) {
  if (date instanceof Date) {
    return date.getMinutes();
  }
  if (typeof date === "string") {
    const datePieces = date.split(":");
    if (datePieces.length >= 2) {
      const minutesString = datePieces[1] || "0";
      const minutes = Number.parseInt(minutesString, 10);
      if (!Number.isNaN(minutes)) {
        return minutes;
      }
    }
  }
  throw new Error(`Failed to get minutes from date: ${date}.`);
}
function getSeconds(date) {
  if (date instanceof Date) {
    return date.getSeconds();
  }
  if (typeof date === "string") {
    const datePieces = date.split(":");
    if (datePieces.length >= 2) {
      const secondsWithMillisecondsString = datePieces[2] || "0";
      const seconds = Number.parseInt(secondsWithMillisecondsString, 10);
      if (!Number.isNaN(seconds)) {
        return seconds;
      }
    }
  }
  throw new Error(`Failed to get seconds from date: ${date}.`);
}
function getMilliseconds(date) {
  if (date instanceof Date) {
    return date.getMilliseconds();
  }
  if (typeof date === "string") {
    const datePieces = date.split(":");
    if (datePieces.length >= 2) {
      const secondsWithMillisecondsString = datePieces[2] || "0";
      const millisecondsString = secondsWithMillisecondsString.split(".")[1] || "0";
      const milliseconds = Number.parseInt(millisecondsString, 10);
      if (!Number.isNaN(milliseconds)) {
        return milliseconds;
      }
    }
  }
  throw new Error(`Failed to get seconds from date: ${date}.`);
}
function getCenturyStart(date) {
  const year = getYear(date);
  const centuryStartYear = year + (-year + 1) % 100;
  const centuryStartDate = /* @__PURE__ */ new Date();
  centuryStartDate.setFullYear(centuryStartYear, 0, 1);
  centuryStartDate.setHours(0, 0, 0, 0);
  return centuryStartDate;
}
var getPreviousCenturyStart = makeGetEdgeOfNeighbor(getYear, getCenturyStart, -100);
var getNextCenturyStart = makeGetEdgeOfNeighbor(getYear, getCenturyStart, 100);
var getCenturyEnd = makeGetEnd(getNextCenturyStart);
var getPreviousCenturyEnd = makeGetEdgeOfNeighbor(getYear, getCenturyEnd, -100);
var getNextCenturyEnd = makeGetEdgeOfNeighbor(getYear, getCenturyEnd, 100);
var getCenturyRange = makeGetRange(getCenturyStart, getCenturyEnd);
function getDecadeStart(date) {
  const year = getYear(date);
  const decadeStartYear = year + (-year + 1) % 10;
  const decadeStartDate = /* @__PURE__ */ new Date();
  decadeStartDate.setFullYear(decadeStartYear, 0, 1);
  decadeStartDate.setHours(0, 0, 0, 0);
  return decadeStartDate;
}
var getPreviousDecadeStart = makeGetEdgeOfNeighbor(getYear, getDecadeStart, -10);
var getNextDecadeStart = makeGetEdgeOfNeighbor(getYear, getDecadeStart, 10);
var getDecadeEnd = makeGetEnd(getNextDecadeStart);
var getPreviousDecadeEnd = makeGetEdgeOfNeighbor(getYear, getDecadeEnd, -10);
var getNextDecadeEnd = makeGetEdgeOfNeighbor(getYear, getDecadeEnd, 10);
var getDecadeRange = makeGetRange(getDecadeStart, getDecadeEnd);
function getYearStart(date) {
  const year = getYear(date);
  const yearStartDate = /* @__PURE__ */ new Date();
  yearStartDate.setFullYear(year, 0, 1);
  yearStartDate.setHours(0, 0, 0, 0);
  return yearStartDate;
}
var getPreviousYearStart = makeGetEdgeOfNeighbor(getYear, getYearStart, -1);
var getNextYearStart = makeGetEdgeOfNeighbor(getYear, getYearStart, 1);
var getYearEnd = makeGetEnd(getNextYearStart);
var getPreviousYearEnd = makeGetEdgeOfNeighbor(getYear, getYearEnd, -1);
var getNextYearEnd = makeGetEdgeOfNeighbor(getYear, getYearEnd, 1);
var getYearRange = makeGetRange(getYearStart, getYearEnd);
function makeGetEdgeOfNeighborMonth(getEdgeOfPeriod, defaultOffset) {
  return function makeGetEdgeOfNeighborMonthInternal(date, offset = defaultOffset) {
    const year = getYear(date);
    const month = getMonth(date) + offset;
    const previousPeriod = /* @__PURE__ */ new Date();
    previousPeriod.setFullYear(year, month, 1);
    previousPeriod.setHours(0, 0, 0, 0);
    return getEdgeOfPeriod(previousPeriod);
  };
}
function getMonthStart(date) {
  const year = getYear(date);
  const month = getMonth(date);
  const monthStartDate = /* @__PURE__ */ new Date();
  monthStartDate.setFullYear(year, month, 1);
  monthStartDate.setHours(0, 0, 0, 0);
  return monthStartDate;
}
var getPreviousMonthStart = makeGetEdgeOfNeighborMonth(getMonthStart, -1);
var getNextMonthStart = makeGetEdgeOfNeighborMonth(getMonthStart, 1);
var getMonthEnd = makeGetEnd(getNextMonthStart);
var getPreviousMonthEnd = makeGetEdgeOfNeighborMonth(getMonthEnd, -1);
var getNextMonthEnd = makeGetEdgeOfNeighborMonth(getMonthEnd, 1);
var getMonthRange = makeGetRange(getMonthStart, getMonthEnd);
function makeGetEdgeOfNeighborDay(getEdgeOfPeriod, defaultOffset) {
  return function makeGetEdgeOfNeighborDayInternal(date, offset = defaultOffset) {
    const year = getYear(date);
    const month = getMonth(date);
    const day = getDate(date) + offset;
    const previousPeriod = /* @__PURE__ */ new Date();
    previousPeriod.setFullYear(year, month, day);
    previousPeriod.setHours(0, 0, 0, 0);
    return getEdgeOfPeriod(previousPeriod);
  };
}
function getDayStart(date) {
  const year = getYear(date);
  const month = getMonth(date);
  const day = getDate(date);
  const dayStartDate = /* @__PURE__ */ new Date();
  dayStartDate.setFullYear(year, month, day);
  dayStartDate.setHours(0, 0, 0, 0);
  return dayStartDate;
}
var getPreviousDayStart = makeGetEdgeOfNeighborDay(getDayStart, -1);
var getNextDayStart = makeGetEdgeOfNeighborDay(getDayStart, 1);
var getDayEnd = makeGetEnd(getNextDayStart);
var getPreviousDayEnd = makeGetEdgeOfNeighborDay(getDayEnd, -1);
var getNextDayEnd = makeGetEdgeOfNeighborDay(getDayEnd, 1);
var getDayRange = makeGetRange(getDayStart, getDayEnd);
function getDaysInMonth(date) {
  return getDate(getMonthEnd(date));
}
function padStart(num, val = 2) {
  const numStr = `${num}`;
  if (numStr.length >= val) {
    return num;
  }
  return `0000${numStr}`.slice(-val);
}
function getHoursMinutes(date) {
  const hours = padStart(getHours(date));
  const minutes = padStart(getMinutes(date));
  return `${hours}:${minutes}`;
}
function getHoursMinutesSeconds(date) {
  const hours = padStart(getHours(date));
  const minutes = padStart(getMinutes(date));
  const seconds = padStart(getSeconds(date));
  return `${hours}:${minutes}:${seconds}`;
}
function getISOLocalDate(date) {
  const year = padStart(getYear(date), 4);
  const month = padStart(getMonthHuman(date));
  const day = padStart(getDate(date));
  return `${year}-${month}-${day}`;
}
function getISOLocalDateTime(date) {
  return `${getISOLocalDate(date)}T${getHoursMinutesSeconds(date)}`;
}

// node_modules/react-calendar/dist/shared/const.js
var CALENDAR_TYPES = {
  GREGORY: "gregory",
  HEBREW: "hebrew",
  ISLAMIC: "islamic",
  ISO_8601: "iso8601"
};
var CALENDAR_TYPE_LOCALES = {
  gregory: [
    "en-CA",
    "en-US",
    "es-AR",
    "es-BO",
    "es-CL",
    "es-CO",
    "es-CR",
    "es-DO",
    "es-EC",
    "es-GT",
    "es-HN",
    "es-MX",
    "es-NI",
    "es-PA",
    "es-PE",
    "es-PR",
    "es-SV",
    "es-VE",
    "pt-BR"
  ],
  hebrew: ["he", "he-IL"],
  islamic: [
    // ar-LB, ar-MA intentionally missing
    "ar",
    "ar-AE",
    "ar-BH",
    "ar-DZ",
    "ar-EG",
    "ar-IQ",
    "ar-JO",
    "ar-KW",
    "ar-LY",
    "ar-OM",
    "ar-QA",
    "ar-SA",
    "ar-SD",
    "ar-SY",
    "ar-YE",
    "dv",
    "dv-MV",
    "ps",
    "ps-AR"
  ]
};
var WEEKDAYS = [0, 1, 2, 3, 4, 5, 6];

// node_modules/react-calendar/dist/shared/dates.js
var SUNDAY = WEEKDAYS[0];
var FRIDAY = WEEKDAYS[5];
var SATURDAY = WEEKDAYS[6];
function getDayOfWeek(date, calendarType = CALENDAR_TYPES.ISO_8601) {
  const weekday = date.getDay();
  switch (calendarType) {
    case CALENDAR_TYPES.ISO_8601:
      return (weekday + 6) % 7;
    case CALENDAR_TYPES.ISLAMIC:
      return (weekday + 1) % 7;
    case CALENDAR_TYPES.HEBREW:
    case CALENDAR_TYPES.GREGORY:
      return weekday;
    default:
      throw new Error("Unsupported calendar type.");
  }
}
function getBeginOfCenturyYear(date) {
  const beginOfCentury = getCenturyStart(date);
  return getYear(beginOfCentury);
}
function getBeginOfDecadeYear(date) {
  const beginOfDecade = getDecadeStart(date);
  return getYear(beginOfDecade);
}
function getBeginOfWeek(date, calendarType = CALENDAR_TYPES.ISO_8601) {
  const year = getYear(date);
  const monthIndex = getMonth(date);
  const day = date.getDate() - getDayOfWeek(date, calendarType);
  return new Date(year, monthIndex, day);
}
function getWeekNumber(date, calendarType = CALENDAR_TYPES.ISO_8601) {
  const calendarTypeForWeekNumber = calendarType === CALENDAR_TYPES.GREGORY ? CALENDAR_TYPES.GREGORY : CALENDAR_TYPES.ISO_8601;
  const beginOfWeek = getBeginOfWeek(date, calendarType);
  let year = getYear(date) + 1;
  let dayInWeekOne;
  let beginOfFirstWeek;
  do {
    dayInWeekOne = new Date(year, 0, calendarTypeForWeekNumber === CALENDAR_TYPES.ISO_8601 ? 4 : 1);
    beginOfFirstWeek = getBeginOfWeek(dayInWeekOne, calendarType);
    year -= 1;
  } while (date < beginOfFirstWeek);
  return Math.round((beginOfWeek.getTime() - beginOfFirstWeek.getTime()) / (864e5 * 7)) + 1;
}
function getBegin(rangeType, date) {
  switch (rangeType) {
    case "century":
      return getCenturyStart(date);
    case "decade":
      return getDecadeStart(date);
    case "year":
      return getYearStart(date);
    case "month":
      return getMonthStart(date);
    case "day":
      return getDayStart(date);
    default:
      throw new Error(`Invalid rangeType: ${rangeType}`);
  }
}
function getBeginPrevious(rangeType, date) {
  switch (rangeType) {
    case "century":
      return getPreviousCenturyStart(date);
    case "decade":
      return getPreviousDecadeStart(date);
    case "year":
      return getPreviousYearStart(date);
    case "month":
      return getPreviousMonthStart(date);
    default:
      throw new Error(`Invalid rangeType: ${rangeType}`);
  }
}
function getBeginNext(rangeType, date) {
  switch (rangeType) {
    case "century":
      return getNextCenturyStart(date);
    case "decade":
      return getNextDecadeStart(date);
    case "year":
      return getNextYearStart(date);
    case "month":
      return getNextMonthStart(date);
    default:
      throw new Error(`Invalid rangeType: ${rangeType}`);
  }
}
function getBeginPrevious2(rangeType, date) {
  switch (rangeType) {
    case "decade":
      return getPreviousDecadeStart(date, -100);
    case "year":
      return getPreviousYearStart(date, -10);
    case "month":
      return getPreviousMonthStart(date, -12);
    default:
      throw new Error(`Invalid rangeType: ${rangeType}`);
  }
}
function getBeginNext2(rangeType, date) {
  switch (rangeType) {
    case "decade":
      return getNextDecadeStart(date, 100);
    case "year":
      return getNextYearStart(date, 10);
    case "month":
      return getNextMonthStart(date, 12);
    default:
      throw new Error(`Invalid rangeType: ${rangeType}`);
  }
}
function getEnd(rangeType, date) {
  switch (rangeType) {
    case "century":
      return getCenturyEnd(date);
    case "decade":
      return getDecadeEnd(date);
    case "year":
      return getYearEnd(date);
    case "month":
      return getMonthEnd(date);
    case "day":
      return getDayEnd(date);
    default:
      throw new Error(`Invalid rangeType: ${rangeType}`);
  }
}
function getEndPrevious(rangeType, date) {
  switch (rangeType) {
    case "century":
      return getPreviousCenturyEnd(date);
    case "decade":
      return getPreviousDecadeEnd(date);
    case "year":
      return getPreviousYearEnd(date);
    case "month":
      return getPreviousMonthEnd(date);
    default:
      throw new Error(`Invalid rangeType: ${rangeType}`);
  }
}
function getEndPrevious2(rangeType, date) {
  switch (rangeType) {
    case "decade":
      return getPreviousDecadeEnd(date, -100);
    case "year":
      return getPreviousYearEnd(date, -10);
    case "month":
      return getPreviousMonthEnd(date, -12);
    default:
      throw new Error(`Invalid rangeType: ${rangeType}`);
  }
}
function getRange(rangeType, date) {
  switch (rangeType) {
    case "century":
      return getCenturyRange(date);
    case "decade":
      return getDecadeRange(date);
    case "year":
      return getYearRange(date);
    case "month":
      return getMonthRange(date);
    case "day":
      return getDayRange(date);
    default:
      throw new Error(`Invalid rangeType: ${rangeType}`);
  }
}
function getValueRange(rangeType, date1, date2) {
  const rawNextValue = [date1, date2].sort((a, b) => a.getTime() - b.getTime());
  return [getBegin(rangeType, rawNextValue[0]), getEnd(rangeType, rawNextValue[1])];
}
function toYearLabel(locale, formatYear2, dates) {
  return dates.map((date) => (formatYear2 || formatYear)(locale, date)).join(" – ");
}
function getCenturyLabel(locale, formatYear2, date) {
  return toYearLabel(locale, formatYear2, getCenturyRange(date));
}
function getDecadeLabel(locale, formatYear2, date) {
  return toYearLabel(locale, formatYear2, getDecadeRange(date));
}
function isCurrentDayOfWeek(date) {
  return date.getDay() === (/* @__PURE__ */ new Date()).getDay();
}
function isWeekend(date, calendarType = CALENDAR_TYPES.ISO_8601) {
  const weekday = date.getDay();
  switch (calendarType) {
    case CALENDAR_TYPES.ISLAMIC:
    case CALENDAR_TYPES.HEBREW:
      return weekday === FRIDAY || weekday === SATURDAY;
    case CALENDAR_TYPES.ISO_8601:
    case CALENDAR_TYPES.GREGORY:
      return weekday === SATURDAY || weekday === SUNDAY;
    default:
      throw new Error("Unsupported calendar type.");
  }
}

// node_modules/react-calendar/dist/Calendar/Navigation.js
var className = "react-calendar__navigation";
function Navigation({ activeStartDate, drillUp, formatMonthYear: formatMonthYear2 = formatMonthYear, formatYear: formatYear2 = formatYear, locale, maxDate, minDate, navigationAriaLabel = "", navigationAriaLive, navigationLabel, next2AriaLabel = "", next2Label = "»", nextAriaLabel = "", nextLabel = "›", prev2AriaLabel = "", prev2Label = "«", prevAriaLabel = "", prevLabel = "‹", setActiveStartDate, showDoubleView, view, views }) {
  const drillUpAvailable = views.indexOf(view) > 0;
  const shouldShowPrevNext2Buttons = view !== "century";
  const previousActiveStartDate = getBeginPrevious(view, activeStartDate);
  const previousActiveStartDate2 = shouldShowPrevNext2Buttons ? getBeginPrevious2(view, activeStartDate) : void 0;
  const nextActiveStartDate = getBeginNext(view, activeStartDate);
  const nextActiveStartDate2 = shouldShowPrevNext2Buttons ? getBeginNext2(view, activeStartDate) : void 0;
  const prevButtonDisabled = (() => {
    if (previousActiveStartDate.getFullYear() < 0) {
      return true;
    }
    const previousActiveEndDate = getEndPrevious(view, activeStartDate);
    return minDate && minDate >= previousActiveEndDate;
  })();
  const prev2ButtonDisabled = shouldShowPrevNext2Buttons && (() => {
    if (previousActiveStartDate2.getFullYear() < 0) {
      return true;
    }
    const previousActiveEndDate = getEndPrevious2(view, activeStartDate);
    return minDate && minDate >= previousActiveEndDate;
  })();
  const nextButtonDisabled = maxDate && maxDate < nextActiveStartDate;
  const next2ButtonDisabled = shouldShowPrevNext2Buttons && maxDate && maxDate < nextActiveStartDate2;
  function onClickPrevious() {
    setActiveStartDate(previousActiveStartDate, "prev");
  }
  function onClickPrevious2() {
    setActiveStartDate(previousActiveStartDate2, "prev2");
  }
  function onClickNext() {
    setActiveStartDate(nextActiveStartDate, "next");
  }
  function onClickNext2() {
    setActiveStartDate(nextActiveStartDate2, "next2");
  }
  function renderLabel(date) {
    const label = (() => {
      switch (view) {
        case "century":
          return getCenturyLabel(locale, formatYear2, date);
        case "decade":
          return getDecadeLabel(locale, formatYear2, date);
        case "year":
          return formatYear2(locale, date);
        case "month":
          return formatMonthYear2(locale, date);
        default:
          throw new Error(`Invalid view: ${view}.`);
      }
    })();
    return navigationLabel ? navigationLabel({
      date,
      label,
      locale: locale || getUserLocale() || void 0,
      view
    }) : label;
  }
  function renderButton() {
    const labelClassName = `${className}__label`;
    return (0, import_jsx_runtime.jsxs)("button", { "aria-label": navigationAriaLabel, "aria-live": navigationAriaLive, className: labelClassName, disabled: !drillUpAvailable, onClick: drillUp, style: { flexGrow: 1 }, type: "button", children: [(0, import_jsx_runtime.jsx)("span", { className: `${labelClassName}__labelText ${labelClassName}__labelText--from`, children: renderLabel(activeStartDate) }), showDoubleView ? (0, import_jsx_runtime.jsxs)(import_jsx_runtime.Fragment, { children: [(0, import_jsx_runtime.jsx)("span", { className: `${labelClassName}__divider`, children: " – " }), (0, import_jsx_runtime.jsx)("span", { className: `${labelClassName}__labelText ${labelClassName}__labelText--to`, children: renderLabel(nextActiveStartDate) })] }) : null] });
  }
  return (0, import_jsx_runtime.jsxs)("div", { className, children: [prev2Label !== null && shouldShowPrevNext2Buttons ? (0, import_jsx_runtime.jsx)("button", { "aria-label": prev2AriaLabel, className: `${className}__arrow ${className}__prev2-button`, disabled: prev2ButtonDisabled, onClick: onClickPrevious2, type: "button", children: prev2Label }) : null, prevLabel !== null && (0, import_jsx_runtime.jsx)("button", { "aria-label": prevAriaLabel, className: `${className}__arrow ${className}__prev-button`, disabled: prevButtonDisabled, onClick: onClickPrevious, type: "button", children: prevLabel }), renderButton(), nextLabel !== null && (0, import_jsx_runtime.jsx)("button", { "aria-label": nextAriaLabel, className: `${className}__arrow ${className}__next-button`, disabled: nextButtonDisabled, onClick: onClickNext, type: "button", children: nextLabel }), next2Label !== null && shouldShowPrevNext2Buttons ? (0, import_jsx_runtime.jsx)("button", { "aria-label": next2AriaLabel, className: `${className}__arrow ${className}__next2-button`, disabled: next2ButtonDisabled, onClick: onClickNext2, type: "button", children: next2Label }) : null] });
}

// node_modules/react-calendar/dist/Calendar.js
var import_jsx_runtime20 = __toESM(require_jsx_runtime(), 1);
var import_react3 = __toESM(require_react(), 1);

// node_modules/react-calendar/dist/CenturyView.js
var import_jsx_runtime7 = __toESM(require_jsx_runtime(), 1);

// node_modules/react-calendar/dist/CenturyView/Decades.js
var import_jsx_runtime6 = __toESM(require_jsx_runtime(), 1);

// node_modules/react-calendar/dist/TileGroup.js
var import_jsx_runtime3 = __toESM(require_jsx_runtime(), 1);

// node_modules/react-calendar/dist/Flex.js
var import_jsx_runtime2 = __toESM(require_jsx_runtime(), 1);
var import_react = __toESM(require_react(), 1);
function toPercent(num) {
  return `${num}%`;
}
function Flex({ children, className: className8, count, direction, offset, style, wrap, ...otherProps }) {
  return (0, import_jsx_runtime2.jsx)("div", { className: className8, style: {
    display: "flex",
    flexDirection: direction,
    flexWrap: wrap ? "wrap" : "nowrap",
    ...style
  }, ...otherProps, children: import_react.Children.map(children, (child, index) => {
    const marginInlineStart = offset && index === 0 ? toPercent(100 * offset / count) : null;
    return (0, import_react.cloneElement)(child, {
      ...child.props,
      style: {
        flexBasis: toPercent(100 / count),
        flexShrink: 0,
        flexGrow: 0,
        overflow: "hidden",
        marginLeft: marginInlineStart,
        marginInlineStart,
        marginInlineEnd: 0
      }
    });
  }) });
}

// node_modules/react-calendar/dist/shared/utils.js
function between(value, min, max) {
  if (min && min > value) {
    return min;
  }
  if (max && max < value) {
    return max;
  }
  return value;
}
function isValueWithinRange(value, range) {
  return range[0] <= value && range[1] >= value;
}
function isRangeWithinRange(greaterRange, smallerRange) {
  return greaterRange[0] <= smallerRange[0] && greaterRange[1] >= smallerRange[1];
}
function doRangesOverlap(range1, range2) {
  return isValueWithinRange(range1[0], range2) || isValueWithinRange(range1[1], range2);
}
function getRangeClassNames(valueRange, dateRange, baseClassName3) {
  const isRange = doRangesOverlap(dateRange, valueRange);
  const classes = [];
  if (isRange) {
    classes.push(baseClassName3);
    const isRangeStart = isValueWithinRange(valueRange[0], dateRange);
    const isRangeEnd = isValueWithinRange(valueRange[1], dateRange);
    if (isRangeStart) {
      classes.push(`${baseClassName3}Start`);
    }
    if (isRangeEnd) {
      classes.push(`${baseClassName3}End`);
    }
    if (isRangeStart && isRangeEnd) {
      classes.push(`${baseClassName3}BothEnds`);
    }
  }
  return classes;
}
function isCompleteValue(value) {
  if (Array.isArray(value)) {
    return value[0] !== null && value[1] !== null;
  }
  return value !== null;
}
function getTileClasses(args) {
  if (!args) {
    throw new Error("args is required");
  }
  const { value, date, hover } = args;
  const className8 = "react-calendar__tile";
  const classes = [className8];
  if (!date) {
    return classes;
  }
  const now = /* @__PURE__ */ new Date();
  const dateRange = (() => {
    if (Array.isArray(date)) {
      return date;
    }
    const { dateType } = args;
    if (!dateType) {
      throw new Error("dateType is required when date is not an array of two dates");
    }
    return getRange(dateType, date);
  })();
  if (isValueWithinRange(now, dateRange)) {
    classes.push(`${className8}--now`);
  }
  if (!value || !isCompleteValue(value)) {
    return classes;
  }
  const valueRange = (() => {
    if (Array.isArray(value)) {
      return value;
    }
    const { valueType } = args;
    if (!valueType) {
      throw new Error("valueType is required when value is not an array of two dates");
    }
    return getRange(valueType, value);
  })();
  if (isRangeWithinRange(valueRange, dateRange)) {
    classes.push(`${className8}--active`);
  } else if (doRangesOverlap(valueRange, dateRange)) {
    classes.push(`${className8}--hasActive`);
  }
  const valueRangeClassNames = getRangeClassNames(valueRange, dateRange, `${className8}--range`);
  classes.push(...valueRangeClassNames);
  const valueArray = Array.isArray(value) ? value : [value];
  if (hover && valueArray.length === 1) {
    const hoverRange = hover > valueRange[0] ? [valueRange[0], hover] : [hover, valueRange[0]];
    const hoverRangeClassNames = getRangeClassNames(hoverRange, dateRange, `${className8}--hover`);
    classes.push(...hoverRangeClassNames);
  }
  return classes;
}

// node_modules/react-calendar/dist/TileGroup.js
function TileGroup({ className: className8, count = 3, dateTransform, dateType, end, hover, offset, renderTile, start, step = 1, value, valueType }) {
  const tiles = [];
  for (let point = start; point <= end; point += step) {
    const date = dateTransform(point);
    tiles.push(renderTile({
      classes: getTileClasses({
        date,
        dateType,
        hover,
        value,
        valueType
      }),
      date
    }));
  }
  return (0, import_jsx_runtime3.jsx)(Flex, { className: className8, count, offset, wrap: true, children: tiles });
}

// node_modules/react-calendar/dist/CenturyView/Decade.js
var import_jsx_runtime5 = __toESM(require_jsx_runtime(), 1);

// node_modules/react-calendar/dist/Tile.js
var import_jsx_runtime4 = __toESM(require_jsx_runtime(), 1);
var import_react2 = __toESM(require_react(), 1);
function Tile(props) {
  const { activeStartDate, children, classes, date, formatAbbr, locale, maxDate, maxDateTransform, minDate, minDateTransform, onClick, onMouseOver, style, tileClassName: tileClassNameProps, tileContent: tileContentProps, tileDisabled, view } = props;
  const tileClassName = (0, import_react2.useMemo)(() => {
    const args = { activeStartDate, date, view };
    return typeof tileClassNameProps === "function" ? tileClassNameProps(args) : tileClassNameProps;
  }, [activeStartDate, date, tileClassNameProps, view]);
  const tileContent = (0, import_react2.useMemo)(() => {
    const args = { activeStartDate, date, view };
    return typeof tileContentProps === "function" ? tileContentProps(args) : tileContentProps;
  }, [activeStartDate, date, tileContentProps, view]);
  return (0, import_jsx_runtime4.jsxs)("button", { className: clsx_default(classes, tileClassName), disabled: minDate && minDateTransform(minDate) > date || maxDate && maxDateTransform(maxDate) < date || (tileDisabled === null || tileDisabled === void 0 ? void 0 : tileDisabled({ activeStartDate, date, view })), onClick: onClick ? (event) => onClick(date, event) : void 0, onFocus: onMouseOver ? () => onMouseOver(date) : void 0, onMouseOver: onMouseOver ? () => onMouseOver(date) : void 0, style, type: "button", children: [formatAbbr ? (0, import_jsx_runtime4.jsx)("abbr", { "aria-label": formatAbbr(locale, date), children }) : children, tileContent] });
}

// node_modules/react-calendar/dist/CenturyView/Decade.js
var className2 = "react-calendar__century-view__decades__decade";
function Decade({ classes = [], currentCentury, formatYear: formatYear2 = formatYear, ...otherProps }) {
  const { date, locale } = otherProps;
  const classesProps = [];
  if (classes) {
    classesProps.push(...classes);
  }
  if (className2) {
    classesProps.push(className2);
  }
  if (getCenturyStart(date).getFullYear() !== currentCentury) {
    classesProps.push(`${className2}--neighboringCentury`);
  }
  return (0, import_jsx_runtime5.jsx)(Tile, { ...otherProps, classes: classesProps, maxDateTransform: getDecadeEnd, minDateTransform: getDecadeStart, view: "century", children: getDecadeLabel(locale, formatYear2, date) });
}

// node_modules/react-calendar/dist/CenturyView/Decades.js
function Decades(props) {
  const { activeStartDate, hover, showNeighboringCentury, value, valueType, ...otherProps } = props;
  const start = getBeginOfCenturyYear(activeStartDate);
  const end = start + (showNeighboringCentury ? 119 : 99);
  return (0, import_jsx_runtime6.jsx)(TileGroup, { className: "react-calendar__century-view__decades", dateTransform: getDecadeStart, dateType: "decade", end, hover, renderTile: ({ date, ...otherTileProps }) => (0, import_jsx_runtime6.jsx)(Decade, { ...otherProps, ...otherTileProps, activeStartDate, currentCentury: start, date }, date.getTime()), start, step: 10, value, valueType });
}

// node_modules/react-calendar/dist/CenturyView.js
function CenturyView(props) {
  function renderDecades() {
    return (0, import_jsx_runtime7.jsx)(Decades, { ...props });
  }
  return (0, import_jsx_runtime7.jsx)("div", { className: "react-calendar__century-view", children: renderDecades() });
}

// node_modules/react-calendar/dist/DecadeView.js
var import_jsx_runtime10 = __toESM(require_jsx_runtime(), 1);

// node_modules/react-calendar/dist/DecadeView/Years.js
var import_jsx_runtime9 = __toESM(require_jsx_runtime(), 1);

// node_modules/react-calendar/dist/DecadeView/Year.js
var import_jsx_runtime8 = __toESM(require_jsx_runtime(), 1);
var className3 = "react-calendar__decade-view__years__year";
function Year({ classes = [], currentDecade, formatYear: formatYear2 = formatYear, ...otherProps }) {
  const { date, locale } = otherProps;
  const classesProps = [];
  if (classes) {
    classesProps.push(...classes);
  }
  if (className3) {
    classesProps.push(className3);
  }
  if (getDecadeStart(date).getFullYear() !== currentDecade) {
    classesProps.push(`${className3}--neighboringDecade`);
  }
  return (0, import_jsx_runtime8.jsx)(Tile, { ...otherProps, classes: classesProps, maxDateTransform: getYearEnd, minDateTransform: getYearStart, view: "decade", children: formatYear2(locale, date) });
}

// node_modules/react-calendar/dist/DecadeView/Years.js
function Years(props) {
  const { activeStartDate, hover, showNeighboringDecade, value, valueType, ...otherProps } = props;
  const start = getBeginOfDecadeYear(activeStartDate);
  const end = start + (showNeighboringDecade ? 11 : 9);
  return (0, import_jsx_runtime9.jsx)(TileGroup, { className: "react-calendar__decade-view__years", dateTransform: getYearStart, dateType: "year", end, hover, renderTile: ({ date, ...otherTileProps }) => (0, import_jsx_runtime9.jsx)(Year, { ...otherProps, ...otherTileProps, activeStartDate, currentDecade: start, date }, date.getTime()), start, value, valueType });
}

// node_modules/react-calendar/dist/DecadeView.js
function DecadeView(props) {
  function renderYears() {
    return (0, import_jsx_runtime10.jsx)(Years, { ...props });
  }
  return (0, import_jsx_runtime10.jsx)("div", { className: "react-calendar__decade-view", children: renderYears() });
}

// node_modules/react-calendar/dist/MonthView.js
var import_jsx_runtime16 = __toESM(require_jsx_runtime(), 1);

// node_modules/react-calendar/dist/MonthView/Days.js
var import_jsx_runtime12 = __toESM(require_jsx_runtime(), 1);

// node_modules/react-calendar/dist/MonthView/Day.js
var import_jsx_runtime11 = __toESM(require_jsx_runtime(), 1);
var className4 = "react-calendar__month-view__days__day";
function Day({ calendarType, classes = [], currentMonthIndex, formatDay: formatDay2 = formatDay, formatLongDate: formatLongDate2 = formatLongDate, ...otherProps }) {
  const { date, locale } = otherProps;
  const classesProps = [];
  if (classes) {
    classesProps.push(...classes);
  }
  if (className4) {
    classesProps.push(className4);
  }
  if (isWeekend(date, calendarType)) {
    classesProps.push(`${className4}--weekend`);
  }
  if (date.getMonth() !== currentMonthIndex) {
    classesProps.push(`${className4}--neighboringMonth`);
  }
  return (0, import_jsx_runtime11.jsx)(Tile, { ...otherProps, classes: classesProps, formatAbbr: formatLongDate2, maxDateTransform: getDayEnd, minDateTransform: getDayStart, view: "month", children: formatDay2(locale, date) });
}

// node_modules/react-calendar/dist/MonthView/Days.js
function Days(props) {
  const { activeStartDate, calendarType, hover, showFixedNumberOfWeeks, showNeighboringMonth, value, valueType, ...otherProps } = props;
  const year = getYear(activeStartDate);
  const monthIndex = getMonth(activeStartDate);
  const hasFixedNumberOfWeeks = showFixedNumberOfWeeks || showNeighboringMonth;
  const dayOfWeek = getDayOfWeek(activeStartDate, calendarType);
  const offset = hasFixedNumberOfWeeks ? 0 : dayOfWeek;
  const start = (hasFixedNumberOfWeeks ? -dayOfWeek : 0) + 1;
  const end = (() => {
    if (showFixedNumberOfWeeks) {
      return start + 6 * 7 - 1;
    }
    const daysInMonth = getDaysInMonth(activeStartDate);
    if (showNeighboringMonth) {
      const activeEndDate = /* @__PURE__ */ new Date();
      activeEndDate.setFullYear(year, monthIndex, daysInMonth);
      activeEndDate.setHours(0, 0, 0, 0);
      const daysUntilEndOfTheWeek = 7 - getDayOfWeek(activeEndDate, calendarType) - 1;
      return daysInMonth + daysUntilEndOfTheWeek;
    }
    return daysInMonth;
  })();
  return (0, import_jsx_runtime12.jsx)(TileGroup, { className: "react-calendar__month-view__days", count: 7, dateTransform: (day) => {
    const date = /* @__PURE__ */ new Date();
    date.setFullYear(year, monthIndex, day);
    return getDayStart(date);
  }, dateType: "day", hover, end, renderTile: ({ date, ...otherTileProps }) => (0, import_jsx_runtime12.jsx)(Day, { ...otherProps, ...otherTileProps, activeStartDate, calendarType, currentMonthIndex: monthIndex, date }, date.getTime()), offset, start, value, valueType });
}

// node_modules/react-calendar/dist/MonthView/Weekdays.js
var import_jsx_runtime13 = __toESM(require_jsx_runtime(), 1);
var className5 = "react-calendar__month-view__weekdays";
var weekdayClassName = `${className5}__weekday`;
function Weekdays(props) {
  const { calendarType, formatShortWeekday: formatShortWeekday2 = formatShortWeekday, formatWeekday: formatWeekday2 = formatWeekday, locale, onMouseLeave } = props;
  const anyDate = /* @__PURE__ */ new Date();
  const beginOfMonth = getMonthStart(anyDate);
  const year = getYear(beginOfMonth);
  const monthIndex = getMonth(beginOfMonth);
  const weekdays = [];
  for (let weekday = 1; weekday <= 7; weekday += 1) {
    const weekdayDate = new Date(year, monthIndex, weekday - getDayOfWeek(beginOfMonth, calendarType));
    const abbr = formatWeekday2(locale, weekdayDate);
    weekdays.push((0, import_jsx_runtime13.jsx)("div", { className: clsx_default(weekdayClassName, isCurrentDayOfWeek(weekdayDate) && `${weekdayClassName}--current`, isWeekend(weekdayDate, calendarType) && `${weekdayClassName}--weekend`), children: (0, import_jsx_runtime13.jsx)("abbr", { "aria-label": abbr, title: abbr, children: formatShortWeekday2(locale, weekdayDate).replace(".", "") }) }, weekday));
  }
  return (0, import_jsx_runtime13.jsx)(Flex, { className: className5, count: 7, onFocus: onMouseLeave, onMouseOver: onMouseLeave, children: weekdays });
}

// node_modules/react-calendar/dist/MonthView/WeekNumbers.js
var import_jsx_runtime15 = __toESM(require_jsx_runtime(), 1);

// node_modules/react-calendar/dist/MonthView/WeekNumber.js
var import_jsx_runtime14 = __toESM(require_jsx_runtime(), 1);
var className6 = "react-calendar__tile";
function WeekNumber(props) {
  const { onClickWeekNumber, weekNumber } = props;
  const children = (0, import_jsx_runtime14.jsx)("span", { children: weekNumber });
  if (onClickWeekNumber) {
    const { date, onClickWeekNumber: onClickWeekNumber2, weekNumber: weekNumber2, ...otherProps } = props;
    return (0, import_jsx_runtime14.jsx)("button", { ...otherProps, className: className6, onClick: (event) => onClickWeekNumber2(weekNumber2, date, event), type: "button", children });
  } else {
    const { date, onClickWeekNumber: onClickWeekNumber2, weekNumber: weekNumber2, ...otherProps } = props;
    return (0, import_jsx_runtime14.jsx)("div", { ...otherProps, className: className6, children });
  }
}

// node_modules/react-calendar/dist/MonthView/WeekNumbers.js
function WeekNumbers(props) {
  const { activeStartDate, calendarType, onClickWeekNumber, onMouseLeave, showFixedNumberOfWeeks } = props;
  const numberOfWeeks = (() => {
    if (showFixedNumberOfWeeks) {
      return 6;
    }
    const numberOfDays = getDaysInMonth(activeStartDate);
    const startWeekday = getDayOfWeek(activeStartDate, calendarType);
    const days = numberOfDays - (7 - startWeekday);
    return 1 + Math.ceil(days / 7);
  })();
  const dates = (() => {
    const year = getYear(activeStartDate);
    const monthIndex = getMonth(activeStartDate);
    const day = getDate(activeStartDate);
    const result = [];
    for (let index = 0; index < numberOfWeeks; index += 1) {
      result.push(getBeginOfWeek(new Date(year, monthIndex, day + index * 7), calendarType));
    }
    return result;
  })();
  const weekNumbers = dates.map((date) => getWeekNumber(date, calendarType));
  return (0, import_jsx_runtime15.jsx)(Flex, { className: "react-calendar__month-view__weekNumbers", count: numberOfWeeks, direction: "column", onFocus: onMouseLeave, onMouseOver: onMouseLeave, style: { flexBasis: "calc(100% * (1 / 8)", flexShrink: 0 }, children: weekNumbers.map((weekNumber, weekIndex) => {
    const date = dates[weekIndex];
    if (!date) {
      throw new Error("date is not defined");
    }
    return (0, import_jsx_runtime15.jsx)(WeekNumber, { date, onClickWeekNumber, weekNumber }, weekNumber);
  }) });
}

// node_modules/react-calendar/dist/MonthView.js
function getCalendarTypeFromLocale(locale) {
  if (locale) {
    for (const [calendarType, locales] of Object.entries(CALENDAR_TYPE_LOCALES)) {
      if (locales.includes(locale)) {
        return calendarType;
      }
    }
  }
  return CALENDAR_TYPES.ISO_8601;
}
function MonthView(props) {
  const { activeStartDate, locale, onMouseLeave, showFixedNumberOfWeeks } = props;
  const { calendarType = getCalendarTypeFromLocale(locale), formatShortWeekday: formatShortWeekday2, formatWeekday: formatWeekday2, onClickWeekNumber, showWeekNumbers, ...childProps } = props;
  function renderWeekdays() {
    return (0, import_jsx_runtime16.jsx)(Weekdays, { calendarType, formatShortWeekday: formatShortWeekday2, formatWeekday: formatWeekday2, locale, onMouseLeave });
  }
  function renderWeekNumbers() {
    if (!showWeekNumbers) {
      return null;
    }
    return (0, import_jsx_runtime16.jsx)(WeekNumbers, { activeStartDate, calendarType, onClickWeekNumber, onMouseLeave, showFixedNumberOfWeeks });
  }
  function renderDays() {
    return (0, import_jsx_runtime16.jsx)(Days, { calendarType, ...childProps });
  }
  const className8 = "react-calendar__month-view";
  return (0, import_jsx_runtime16.jsx)("div", { className: clsx_default(className8, showWeekNumbers ? `${className8}--weekNumbers` : ""), children: (0, import_jsx_runtime16.jsxs)("div", { style: {
    display: "flex",
    alignItems: "flex-end"
  }, children: [renderWeekNumbers(), (0, import_jsx_runtime16.jsxs)("div", { style: {
    flexGrow: 1,
    width: "100%"
  }, children: [renderWeekdays(), renderDays()] })] }) });
}

// node_modules/react-calendar/dist/YearView.js
var import_jsx_runtime19 = __toESM(require_jsx_runtime(), 1);

// node_modules/react-calendar/dist/YearView/Months.js
var import_jsx_runtime18 = __toESM(require_jsx_runtime(), 1);

// node_modules/react-calendar/dist/YearView/Month.js
var import_jsx_runtime17 = __toESM(require_jsx_runtime(), 1);
var className7 = "react-calendar__year-view__months__month";
function Month({ classes = [], formatMonth: formatMonth3 = formatMonth, formatMonthYear: formatMonthYear2 = formatMonthYear, ...otherProps }) {
  const { date, locale } = otherProps;
  return (0, import_jsx_runtime17.jsx)(Tile, { ...otherProps, classes: [...classes, className7], formatAbbr: formatMonthYear2, maxDateTransform: getMonthEnd, minDateTransform: getMonthStart, view: "year", children: formatMonth3(locale, date) });
}

// node_modules/react-calendar/dist/YearView/Months.js
function Months(props) {
  const { activeStartDate, hover, value, valueType, ...otherProps } = props;
  const start = 0;
  const end = 11;
  const year = getYear(activeStartDate);
  return (0, import_jsx_runtime18.jsx)(TileGroup, { className: "react-calendar__year-view__months", dateTransform: (monthIndex) => {
    const date = /* @__PURE__ */ new Date();
    date.setFullYear(year, monthIndex, 1);
    return getMonthStart(date);
  }, dateType: "month", end, hover, renderTile: ({ date, ...otherTileProps }) => (0, import_jsx_runtime18.jsx)(Month, { ...otherProps, ...otherTileProps, activeStartDate, date }, date.getTime()), start, value, valueType });
}

// node_modules/react-calendar/dist/YearView.js
function YearView(props) {
  function renderMonths() {
    return (0, import_jsx_runtime19.jsx)(Months, { ...props });
  }
  return (0, import_jsx_runtime19.jsx)("div", { className: "react-calendar__year-view", children: renderMonths() });
}

// node_modules/react-calendar/dist/Calendar.js
var baseClassName = "react-calendar";
var allViews = ["century", "decade", "year", "month"];
var allValueTypes = ["decade", "year", "month", "day"];
var defaultMinDate = /* @__PURE__ */ new Date();
defaultMinDate.setFullYear(1, 0, 1);
defaultMinDate.setHours(0, 0, 0, 0);
var defaultMaxDate = /* @__PURE__ */ new Date(864e13);
function toDate(value) {
  if (value instanceof Date) {
    return value;
  }
  return new Date(value);
}
function getLimitedViews(minDetail, maxDetail) {
  return allViews.slice(allViews.indexOf(minDetail), allViews.indexOf(maxDetail) + 1);
}
function isViewAllowed(view, minDetail, maxDetail) {
  const views = getLimitedViews(minDetail, maxDetail);
  return views.indexOf(view) !== -1;
}
function getView(view, minDetail, maxDetail) {
  if (!view) {
    return maxDetail;
  }
  if (isViewAllowed(view, minDetail, maxDetail)) {
    return view;
  }
  return maxDetail;
}
function getValueType(view) {
  const index = allViews.indexOf(view);
  return allValueTypes[index];
}
function getValue(value, index) {
  const rawValue = Array.isArray(value) ? value[index] : value;
  if (!rawValue) {
    return null;
  }
  const valueDate = toDate(rawValue);
  if (Number.isNaN(valueDate.getTime())) {
    throw new Error(`Invalid date: ${value}`);
  }
  return valueDate;
}
function getDetailValue({ value, minDate, maxDate, maxDetail }, index) {
  const valuePiece = getValue(value, index);
  if (!valuePiece) {
    return null;
  }
  const valueType = getValueType(maxDetail);
  const detailValueFrom = (() => {
    switch (index) {
      case 0:
        return getBegin(valueType, valuePiece);
      case 1:
        return getEnd(valueType, valuePiece);
      default:
        throw new Error(`Invalid index value: ${index}`);
    }
  })();
  return between(detailValueFrom, minDate, maxDate);
}
var getDetailValueFrom = (args) => getDetailValue(args, 0);
var getDetailValueTo = (args) => getDetailValue(args, 1);
var getDetailValueArray = (args) => [getDetailValueFrom, getDetailValueTo].map((fn) => fn(args));
function getActiveStartDate({ maxDate, maxDetail, minDate, minDetail, value, view }) {
  const rangeType = getView(view, minDetail, maxDetail);
  const valueFrom = getDetailValueFrom({
    value,
    minDate,
    maxDate,
    maxDetail
  }) || /* @__PURE__ */ new Date();
  return getBegin(rangeType, valueFrom);
}
function getInitialActiveStartDate({ activeStartDate, defaultActiveStartDate, defaultValue, defaultView, maxDate, maxDetail, minDate, minDetail, value, view }) {
  const rangeType = getView(view, minDetail, maxDetail);
  const valueFrom = activeStartDate || defaultActiveStartDate;
  if (valueFrom) {
    return getBegin(rangeType, valueFrom);
  }
  return getActiveStartDate({
    maxDate,
    maxDetail,
    minDate,
    minDetail,
    value: value || defaultValue,
    view: view || defaultView
  });
}
function getIsSingleValue(value) {
  return value && (!Array.isArray(value) || value.length === 1);
}
function areDatesEqual(date1, date2) {
  return date1 instanceof Date && date2 instanceof Date && date1.getTime() === date2.getTime();
}
var Calendar = (0, import_react3.forwardRef)(function Calendar2(props, ref) {
  const { activeStartDate: activeStartDateProps, allowPartialRange, calendarType, className: className8, "data-testid": dataTestId, defaultActiveStartDate, defaultValue, defaultView, formatDay: formatDay2, formatLongDate: formatLongDate2, formatMonth: formatMonth3, formatMonthYear: formatMonthYear2, formatShortWeekday: formatShortWeekday2, formatWeekday: formatWeekday2, formatYear: formatYear2, goToRangeStartOnSelect = true, inputRef, locale, maxDate = defaultMaxDate, maxDetail = "month", minDate = defaultMinDate, minDetail = "century", navigationAriaLabel, navigationAriaLive, navigationLabel, next2AriaLabel, next2Label, nextAriaLabel, nextLabel, onActiveStartDateChange, onChange: onChangeProps, onClickDay, onClickDecade, onClickMonth, onClickWeekNumber, onClickYear, onDrillDown, onDrillUp, onViewChange, prev2AriaLabel, prev2Label, prevAriaLabel, prevLabel, returnValue = "start", selectRange, showDoubleView, showFixedNumberOfWeeks, showNavigation = true, showNeighboringCentury, showNeighboringDecade, showNeighboringMonth = true, showWeekNumbers, tileClassName, tileContent, tileDisabled, value: valueProps, view: viewProps } = props;
  const [activeStartDateState, setActiveStartDateState] = (0, import_react3.useState)(defaultActiveStartDate);
  const [hoverState, setHoverState] = (0, import_react3.useState)(null);
  const [valueState, setValueState] = (0, import_react3.useState)(Array.isArray(defaultValue) ? defaultValue.map((el) => el !== null ? toDate(el) : null) : defaultValue !== null && defaultValue !== void 0 ? toDate(defaultValue) : null);
  const [viewState, setViewState] = (0, import_react3.useState)(defaultView);
  const activeStartDate = activeStartDateProps || activeStartDateState || getInitialActiveStartDate({
    activeStartDate: activeStartDateProps,
    defaultActiveStartDate,
    defaultValue,
    defaultView,
    maxDate,
    maxDetail,
    minDate,
    minDetail,
    value: valueProps,
    view: viewProps
  });
  const value = (() => {
    const rawValue = (() => {
      if (selectRange && getIsSingleValue(valueState)) {
        return valueState;
      }
      return valueProps !== void 0 ? valueProps : valueState;
    })();
    if (!rawValue) {
      return null;
    }
    return Array.isArray(rawValue) ? rawValue.map((el) => el !== null ? toDate(el) : null) : rawValue !== null ? toDate(rawValue) : null;
  })();
  const valueType = getValueType(maxDetail);
  const view = getView(viewProps || viewState, minDetail, maxDetail);
  const views = getLimitedViews(minDetail, maxDetail);
  const hover = selectRange ? hoverState : null;
  const drillDownAvailable = views.indexOf(view) < views.length - 1;
  const drillUpAvailable = views.indexOf(view) > 0;
  const getProcessedValue = (0, import_react3.useCallback)((value2) => {
    const processFunction = (() => {
      switch (returnValue) {
        case "start":
          return getDetailValueFrom;
        case "end":
          return getDetailValueTo;
        case "range":
          return getDetailValueArray;
        default:
          throw new Error("Invalid returnValue.");
      }
    })();
    return processFunction({
      maxDate,
      maxDetail,
      minDate,
      value: value2
    });
  }, [maxDate, maxDetail, minDate, returnValue]);
  const setActiveStartDate = (0, import_react3.useCallback)((nextActiveStartDate, action) => {
    setActiveStartDateState(nextActiveStartDate);
    const args = {
      action,
      activeStartDate: nextActiveStartDate,
      value,
      view
    };
    if (onActiveStartDateChange && !areDatesEqual(activeStartDate, nextActiveStartDate)) {
      onActiveStartDateChange(args);
    }
  }, [activeStartDate, onActiveStartDateChange, value, view]);
  const onClickTile = (0, import_react3.useCallback)((value2, event) => {
    const callback = (() => {
      switch (view) {
        case "century":
          return onClickDecade;
        case "decade":
          return onClickYear;
        case "year":
          return onClickMonth;
        case "month":
          return onClickDay;
        default:
          throw new Error(`Invalid view: ${view}.`);
      }
    })();
    if (callback)
      callback(value2, event);
  }, [onClickDay, onClickDecade, onClickMonth, onClickYear, view]);
  const drillDown = (0, import_react3.useCallback)((nextActiveStartDate, event) => {
    if (!drillDownAvailable) {
      return;
    }
    onClickTile(nextActiveStartDate, event);
    const nextView = views[views.indexOf(view) + 1];
    if (!nextView) {
      throw new Error("Attempted to drill down from the lowest view.");
    }
    setActiveStartDateState(nextActiveStartDate);
    setViewState(nextView);
    const args = {
      action: "drillDown",
      activeStartDate: nextActiveStartDate,
      value,
      view: nextView
    };
    if (onActiveStartDateChange && !areDatesEqual(activeStartDate, nextActiveStartDate)) {
      onActiveStartDateChange(args);
    }
    if (onViewChange && view !== nextView) {
      onViewChange(args);
    }
    if (onDrillDown) {
      onDrillDown(args);
    }
  }, [
    activeStartDate,
    drillDownAvailable,
    onActiveStartDateChange,
    onClickTile,
    onDrillDown,
    onViewChange,
    value,
    view,
    views
  ]);
  const drillUp = (0, import_react3.useCallback)(() => {
    if (!drillUpAvailable) {
      return;
    }
    const nextView = views[views.indexOf(view) - 1];
    if (!nextView) {
      throw new Error("Attempted to drill up from the highest view.");
    }
    const nextActiveStartDate = getBegin(nextView, activeStartDate);
    setActiveStartDateState(nextActiveStartDate);
    setViewState(nextView);
    const args = {
      action: "drillUp",
      activeStartDate: nextActiveStartDate,
      value,
      view: nextView
    };
    if (onActiveStartDateChange && !areDatesEqual(activeStartDate, nextActiveStartDate)) {
      onActiveStartDateChange(args);
    }
    if (onViewChange && view !== nextView) {
      onViewChange(args);
    }
    if (onDrillUp) {
      onDrillUp(args);
    }
  }, [
    activeStartDate,
    drillUpAvailable,
    onActiveStartDateChange,
    onDrillUp,
    onViewChange,
    value,
    view,
    views
  ]);
  const onChange = (0, import_react3.useCallback)((rawNextValue, event) => {
    const previousValue = value;
    onClickTile(rawNextValue, event);
    const isFirstValueInRange = selectRange && !getIsSingleValue(previousValue);
    let nextValue;
    if (selectRange) {
      if (isFirstValueInRange) {
        nextValue = getBegin(valueType, rawNextValue);
      } else {
        if (!previousValue) {
          throw new Error("previousValue is required");
        }
        if (Array.isArray(previousValue)) {
          throw new Error("previousValue must not be an array");
        }
        nextValue = getValueRange(valueType, previousValue, rawNextValue);
      }
    } else {
      nextValue = getProcessedValue(rawNextValue);
    }
    const nextActiveStartDate = (
      // Range selection turned off
      !selectRange || // Range selection turned on, first value
      isFirstValueInRange || // Range selection turned on, second value, goToRangeStartOnSelect toggled on
      goToRangeStartOnSelect ? getActiveStartDate({
        maxDate,
        maxDetail,
        minDate,
        minDetail,
        value: nextValue,
        view
      }) : null
    );
    event.persist();
    setActiveStartDateState(nextActiveStartDate);
    setValueState(nextValue);
    const args = {
      action: "onChange",
      activeStartDate: nextActiveStartDate,
      value: nextValue,
      view
    };
    if (onActiveStartDateChange && !areDatesEqual(activeStartDate, nextActiveStartDate)) {
      onActiveStartDateChange(args);
    }
    if (onChangeProps) {
      if (selectRange) {
        const isSingleValue = getIsSingleValue(nextValue);
        if (!isSingleValue) {
          onChangeProps(nextValue || null, event);
        } else if (allowPartialRange) {
          if (Array.isArray(nextValue)) {
            throw new Error("value must not be an array");
          }
          onChangeProps([nextValue || null, null], event);
        }
      } else {
        onChangeProps(nextValue || null, event);
      }
    }
  }, [
    activeStartDate,
    allowPartialRange,
    getProcessedValue,
    goToRangeStartOnSelect,
    maxDate,
    maxDetail,
    minDate,
    minDetail,
    onActiveStartDateChange,
    onChangeProps,
    onClickTile,
    selectRange,
    value,
    valueType,
    view
  ]);
  function onMouseOver(nextHover) {
    setHoverState(nextHover);
  }
  function onMouseLeave() {
    setHoverState(null);
  }
  (0, import_react3.useImperativeHandle)(ref, () => ({
    activeStartDate,
    drillDown,
    drillUp,
    onChange,
    setActiveStartDate,
    value,
    view
  }), [activeStartDate, drillDown, drillUp, onChange, setActiveStartDate, value, view]);
  function renderContent(next) {
    const currentActiveStartDate = next ? getBeginNext(view, activeStartDate) : getBegin(view, activeStartDate);
    const onClick = drillDownAvailable ? drillDown : onChange;
    const commonProps = {
      activeStartDate: currentActiveStartDate,
      hover,
      locale,
      maxDate,
      minDate,
      onClick,
      onMouseOver: selectRange ? onMouseOver : void 0,
      tileClassName,
      tileContent,
      tileDisabled,
      value,
      valueType
    };
    switch (view) {
      case "century": {
        return (0, import_jsx_runtime20.jsx)(CenturyView, { formatYear: formatYear2, showNeighboringCentury, ...commonProps });
      }
      case "decade": {
        return (0, import_jsx_runtime20.jsx)(DecadeView, { formatYear: formatYear2, showNeighboringDecade, ...commonProps });
      }
      case "year": {
        return (0, import_jsx_runtime20.jsx)(YearView, { formatMonth: formatMonth3, formatMonthYear: formatMonthYear2, ...commonProps });
      }
      case "month": {
        return (0, import_jsx_runtime20.jsx)(MonthView, { calendarType, formatDay: formatDay2, formatLongDate: formatLongDate2, formatShortWeekday: formatShortWeekday2, formatWeekday: formatWeekday2, onClickWeekNumber, onMouseLeave: selectRange ? onMouseLeave : void 0, showFixedNumberOfWeeks: typeof showFixedNumberOfWeeks !== "undefined" ? showFixedNumberOfWeeks : showDoubleView, showNeighboringMonth, showWeekNumbers, ...commonProps });
      }
      default:
        throw new Error(`Invalid view: ${view}.`);
    }
  }
  function renderNavigation() {
    if (!showNavigation) {
      return null;
    }
    return (0, import_jsx_runtime20.jsx)(Navigation, { activeStartDate, drillUp, formatMonthYear: formatMonthYear2, formatYear: formatYear2, locale, maxDate, minDate, navigationAriaLabel, navigationAriaLive, navigationLabel, next2AriaLabel, next2Label, nextAriaLabel, nextLabel, prev2AriaLabel, prev2Label, prevAriaLabel, prevLabel, setActiveStartDate, showDoubleView, view, views });
  }
  const valueArray = Array.isArray(value) ? value : [value];
  return (0, import_jsx_runtime20.jsxs)("div", { className: clsx_default(baseClassName, selectRange && valueArray.length === 1 && `${baseClassName}--selectRange`, showDoubleView && `${baseClassName}--doubleView`, className8), "data-testid": dataTestId, ref: inputRef, children: [renderNavigation(), (0, import_jsx_runtime20.jsxs)("div", { className: `${baseClassName}__viewContainer`, onBlur: selectRange ? onMouseLeave : void 0, onMouseLeave: selectRange ? onMouseLeave : void 0, children: [renderContent(), showDoubleView ? renderContent(true) : null] })] });
});
var Calendar_default = Calendar;

// node_modules/react-calendar/dist/index.js
var dist_default2 = Calendar_default;

// node_modules/react-clock/dist/Clock.js
var import_jsx_runtime24 = __toESM(require_jsx_runtime(), 1);

// node_modules/react-clock/dist/Hand.js
var import_jsx_runtime21 = __toESM(require_jsx_runtime(), 1);
function Hand(_a) {
  var _b = _a.angle, angle = _b === void 0 ? 0 : _b, name = _a.name, _c = _a.length, length = _c === void 0 ? 100 : _c, _d = _a.oppositeLength, oppositeLength = _d === void 0 ? 10 : _d, _e = _a.width, width = _e === void 0 ? 1 : _e;
  return (0, import_jsx_runtime21.jsx)("div", { className: "react-clock__hand react-clock__".concat(name, "-hand"), style: {
    transform: "rotate(".concat(angle, "deg)")
  }, children: (0, import_jsx_runtime21.jsx)("div", { className: "react-clock__hand__body react-clock__".concat(name, "-hand__body"), style: {
    width: "".concat(width, "px"),
    top: "".concat(50 - length / 2, "%"),
    bottom: "".concat(50 - oppositeLength / 2, "%")
  } }) });
}

// node_modules/react-clock/dist/Mark.js
var import_jsx_runtime22 = __toESM(require_jsx_runtime(), 1);
var import_react4 = __toESM(require_react(), 1);
var Mark = (0, import_react4.memo)(function Mark2(_a) {
  var _b = _a.angle, angle = _b === void 0 ? 0 : _b, _c = _a.length, length = _c === void 0 ? 10 : _c, name = _a.name, _d = _a.width, width = _d === void 0 ? 1 : _d;
  return (0, import_jsx_runtime22.jsx)("div", { className: "react-clock__mark react-clock__".concat(name, "-mark"), style: {
    transform: "rotate(".concat(angle, "deg)")
  }, children: (0, import_jsx_runtime22.jsx)("div", { className: "react-clock__mark__body react-clock__".concat(name, "-mark__body"), style: {
    width: "".concat(width, "px"),
    top: 0,
    bottom: "".concat(100 - length / 2, "%")
  } }) });
});
var Mark_default = Mark;

// node_modules/react-clock/dist/MarkNumber.js
var import_jsx_runtime23 = __toESM(require_jsx_runtime(), 1);
var import_react5 = __toESM(require_react(), 1);
var MarkNumber = (0, import_react5.memo)(function MarkNumber2(_a) {
  var _b = _a.angle, angle = _b === void 0 ? 0 : _b, _c = _a.length, length = _c === void 0 ? 10 : _c, name = _a.name, number = _a.number;
  return (0, import_jsx_runtime23.jsx)("div", { className: "react-clock__mark react-clock__".concat(name, "-mark"), style: {
    transform: "rotate(".concat(angle, "deg)")
  }, children: (0, import_jsx_runtime23.jsx)("div", { className: "react-clock__mark__number", style: {
    transform: "rotate(-".concat(angle, "deg)"),
    top: "".concat(length / 2, "%")
  }, children: number }) });
});
var MarkNumber_default = MarkNumber;

// node_modules/react-clock/dist/shared/hourFormatter.js
function formatHour(locale, hour) {
  return hour.toLocaleString(locale || dist_default() || void 0);
}

// node_modules/react-clock/dist/shared/utils.js
function isValidNumber(num) {
  return num !== null && num !== false && !Number.isNaN(Number(num));
}
function safeMax() {
  var args = [];
  for (var _i = 0; _i < arguments.length; _i++) {
    args[_i] = arguments[_i];
  }
  return Math.max.apply(Math, args.filter(isValidNumber));
}

// node_modules/react-clock/dist/Clock.js
function Clock(_a) {
  var className8 = _a.className, _b = _a.formatHour, formatHour2 = _b === void 0 ? formatHour : _b, _c = _a.hourHandLength, hourHandLength = _c === void 0 ? 50 : _c, hourHandOppositeLength = _a.hourHandOppositeLength, _d = _a.hourHandWidth, hourHandWidth = _d === void 0 ? 4 : _d, _e = _a.hourMarksLength, hourMarksLength = _e === void 0 ? 10 : _e, _f = _a.hourMarksWidth, hourMarksWidth = _f === void 0 ? 3 : _f, locale = _a.locale, _g = _a.minuteHandLength, minuteHandLength = _g === void 0 ? 70 : _g, minuteHandOppositeLength = _a.minuteHandOppositeLength, _h = _a.minuteHandWidth, minuteHandWidth = _h === void 0 ? 2 : _h, _j = _a.minuteMarksLength, minuteMarksLength = _j === void 0 ? 6 : _j, _k = _a.minuteMarksWidth, minuteMarksWidth = _k === void 0 ? 1 : _k, _l = _a.renderHourMarks, renderHourMarks = _l === void 0 ? true : _l, _m = _a.renderMinuteHand, renderMinuteHand = _m === void 0 ? true : _m, _o = _a.renderMinuteMarks, renderMinuteMarks = _o === void 0 ? true : _o, renderNumbers = _a.renderNumbers, _p = _a.renderSecondHand, renderSecondHand = _p === void 0 ? true : _p, _q = _a.secondHandLength, secondHandLength = _q === void 0 ? 90 : _q, secondHandOppositeLength = _a.secondHandOppositeLength, _r = _a.secondHandWidth, secondHandWidth = _r === void 0 ? 1 : _r, _s = _a.size, size = _s === void 0 ? 150 : _s, useMillisecondPrecision = _a.useMillisecondPrecision, value = _a.value;
  function renderMinuteMarksFn() {
    if (!renderMinuteMarks) {
      return null;
    }
    var minuteMarks = [];
    for (var i = 1; i <= 60; i += 1) {
      var isHourMark = renderHourMarks && !(i % 5);
      if (!isHourMark) {
        minuteMarks.push((0, import_jsx_runtime24.jsx)(Mark_default, { angle: i * 6, length: minuteMarksLength, name: "minute", width: minuteMarksWidth }, "minute_".concat(i)));
      }
    }
    return minuteMarks;
  }
  function renderHourMarksFn() {
    if (!renderHourMarks) {
      return null;
    }
    var hourMarks = [];
    for (var i = 1; i <= 12; i += 1) {
      hourMarks.push((0, import_jsx_runtime24.jsx)(Mark_default, { angle: i * 30, length: hourMarksLength, name: "hour", width: hourMarksWidth }, "hour_".concat(i)));
    }
    return hourMarks;
  }
  function renderNumbersFn() {
    if (!renderNumbers) {
      return null;
    }
    var numbers = [];
    for (var i = 1; i <= 12; i += 1) {
      numbers.push((0, import_jsx_runtime24.jsx)(MarkNumber_default, { angle: i * 30, length: safeMax(renderHourMarks && hourMarksLength, renderMinuteMarks && minuteMarksLength, 0), name: "number", number: formatHour2(locale, i) }, "number_".concat(i)));
    }
    return numbers;
  }
  function renderFace() {
    return (0, import_jsx_runtime24.jsxs)("div", { className: "react-clock__face", children: [renderMinuteMarksFn(), renderHourMarksFn(), renderNumbersFn()] });
  }
  function renderHourHandFn() {
    var angle = value ? getHours(value) * 30 + getMinutes(value) / 2 + getSeconds(value) / 120 + (useMillisecondPrecision ? getMilliseconds(value) / 12e4 : 0) : 0;
    return (0, import_jsx_runtime24.jsx)(Hand, { angle, length: hourHandLength, name: "hour", oppositeLength: hourHandOppositeLength, width: hourHandWidth });
  }
  function renderMinuteHandFn() {
    if (!renderMinuteHand) {
      return null;
    }
    var angle = value ? getHours(value) * 360 + getMinutes(value) * 6 + getSeconds(value) / 10 + (useMillisecondPrecision ? getMilliseconds(value) / 1e4 : 0) : 0;
    return (0, import_jsx_runtime24.jsx)(Hand, { angle, length: minuteHandLength, name: "minute", oppositeLength: minuteHandOppositeLength, width: minuteHandWidth });
  }
  function renderSecondHandFn() {
    if (!renderSecondHand) {
      return null;
    }
    var angle = value ? getMinutes(value) * 360 + getSeconds(value) * 6 + (useMillisecondPrecision ? getMilliseconds(value) * 6e-3 : 0) : 0;
    return (0, import_jsx_runtime24.jsx)(Hand, { angle, length: secondHandLength, name: "second", oppositeLength: secondHandOppositeLength, width: secondHandWidth });
  }
  return (0, import_jsx_runtime24.jsxs)("time", { className: clsx_default("react-clock", className8), dateTime: value instanceof Date ? (
    // Returns a string in the format "HH:MM" or "HH:MM:SS"
    value.toLocaleTimeString("en", {
      hourCycle: "h23",
      hour: "2-digit",
      minute: renderMinuteHand ? "2-digit" : void 0,
      second: renderSecondHand ? "2-digit" : void 0
    })
  ) : value || void 0, style: {
    width: size,
    height: size
  }, children: [renderFace(), renderHourHandFn(), renderMinuteHandFn(), renderSecondHandFn()] });
}

// node_modules/react-clock/dist/index.js
var dist_default3 = Clock;

// node_modules/react-fit/dist/Fit.js
var import_jsx_runtime25 = __toESM(require_jsx_runtime(), 1);
var import_react6 = __toESM(require_react(), 1);

// node_modules/detect-element-overflow/dist/index.js
function getRect(element) {
  return element.getBoundingClientRect();
}
function detectElementOverflow(element, container) {
  return {
    get collidedTop() {
      return getRect(element).top < getRect(container).top;
    },
    get collidedBottom() {
      return getRect(element).bottom > getRect(container).bottom;
    },
    get collidedLeft() {
      return getRect(element).left < getRect(container).left;
    },
    get collidedRight() {
      return getRect(element).right > getRect(container).right;
    },
    get overflowTop() {
      return getRect(container).top - getRect(element).top;
    },
    get overflowBottom() {
      return getRect(element).bottom - getRect(container).bottom;
    },
    get overflowLeft() {
      return getRect(container).left - getRect(element).left;
    },
    get overflowRight() {
      return getRect(element).right - getRect(container).right;
    }
  };
}

// node_modules/react-fit/dist/Fit.js
var import_warning = __toESM(require_warning(), 1);
var __rest = function(s, e) {
  var t = {};
  for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
    t[p] = s[p];
  if (s != null && typeof Object.getOwnPropertySymbols === "function")
    for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
      if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
        t[p[i]] = s[p[i]];
    }
  return t;
};
var isBrowser = typeof window !== "undefined";
var isMutationObserverSupported = isBrowser && "MutationObserver" in window;
function capitalize(string) {
  return string.charAt(0).toUpperCase() + string.slice(1);
}
function findScrollContainer(element) {
  let parent = element.parentElement;
  while (parent) {
    const { overflow } = window.getComputedStyle(parent);
    if (overflow.split(" ").every((o) => o === "auto" || o === "scroll")) {
      return parent;
    }
    parent = parent.parentElement;
  }
  return document.documentElement;
}
function alignAxis({ axis, container, element, invertAxis, scrollContainer, secondary, spacing }) {
  const style = window.getComputedStyle(element);
  const parent = container.parentElement;
  if (!parent) {
    return;
  }
  const scrollContainerCollisions = detectElementOverflow(parent, scrollContainer);
  const documentCollisions = detectElementOverflow(parent, document.documentElement);
  const isX = axis === "x";
  const startProperty = isX ? "left" : "top";
  const endProperty = isX ? "right" : "bottom";
  const sizeProperty = isX ? "width" : "height";
  const overflowStartProperty = `overflow${capitalize(startProperty)}`;
  const overflowEndProperty = `overflow${capitalize(endProperty)}`;
  const scrollProperty = `scroll${capitalize(startProperty)}`;
  const uppercasedSizeProperty = capitalize(sizeProperty);
  const offsetSizeProperty = `offset${uppercasedSizeProperty}`;
  const clientSizeProperty = `client${uppercasedSizeProperty}`;
  const minSizeProperty = `min-${sizeProperty}`;
  const scrollbarWidth = scrollContainer[offsetSizeProperty] - scrollContainer[clientSizeProperty];
  const startSpacing = typeof spacing === "object" ? spacing[startProperty] : spacing;
  let availableStartSpace = -Math.max(scrollContainerCollisions[overflowStartProperty], documentCollisions[overflowStartProperty] + document.documentElement[scrollProperty]) - startSpacing;
  const endSpacing = typeof spacing === "object" ? spacing[endProperty] : spacing;
  let availableEndSpace = -Math.max(scrollContainerCollisions[overflowEndProperty], documentCollisions[overflowEndProperty] - document.documentElement[scrollProperty]) - endSpacing - scrollbarWidth;
  if (secondary) {
    availableStartSpace += parent[clientSizeProperty];
    availableEndSpace += parent[clientSizeProperty];
  }
  const offsetSize = element[offsetSizeProperty];
  function displayStart() {
    element.style[startProperty] = "auto";
    element.style[endProperty] = secondary ? "0" : "100%";
  }
  function displayEnd() {
    element.style[startProperty] = secondary ? "0" : "100%";
    element.style[endProperty] = "auto";
  }
  function displayIfFits(availableSpace, display) {
    const fits2 = offsetSize <= availableSpace;
    if (fits2) {
      display();
    }
    return fits2;
  }
  function displayStartIfFits() {
    return displayIfFits(availableStartSpace, displayStart);
  }
  function displayEndIfFits() {
    return displayIfFits(availableEndSpace, displayEnd);
  }
  function displayWhereverShrinkedFits() {
    const moreSpaceStart = availableStartSpace > availableEndSpace;
    const rawMinSize = style.getPropertyValue(minSizeProperty);
    const minSize = rawMinSize ? Number.parseInt(rawMinSize, 10) : null;
    function shrinkToSize(size) {
      (0, import_warning.default)(!minSize || size >= minSize, `<Fit />'s child will not fit anywhere with its current ${minSizeProperty} of ${minSize}px.`);
      const newSize = Math.max(size, minSize || 0);
      (0, import_warning.default)(false, `<Fit />'s child needed to have its ${sizeProperty} decreased to ${newSize}px.`);
      element.style[sizeProperty] = `${newSize}px`;
    }
    if (moreSpaceStart) {
      shrinkToSize(availableStartSpace);
      displayStart();
    } else {
      shrinkToSize(availableEndSpace);
      displayEnd();
    }
  }
  let fits;
  if (invertAxis) {
    fits = displayStartIfFits() || displayEndIfFits();
  } else {
    fits = displayEndIfFits() || displayStartIfFits();
  }
  if (!fits) {
    displayWhereverShrinkedFits();
  }
}
function alignMainAxis(args) {
  alignAxis(args);
}
function alignSecondaryAxis(args) {
  alignAxis(Object.assign(Object.assign({}, args), { axis: args.axis === "x" ? "y" : "x", secondary: true }));
}
function alignBothAxis(args) {
  const { invertAxis, invertSecondaryAxis } = args, commonArgs = __rest(args, ["invertAxis", "invertSecondaryAxis"]);
  alignMainAxis(Object.assign(Object.assign({}, commonArgs), { invertAxis }));
  alignSecondaryAxis(Object.assign(Object.assign({}, commonArgs), { invertAxis: invertSecondaryAxis }));
}
function Fit({ children, invertAxis, invertSecondaryAxis, mainAxis = "y", spacing = 8 }) {
  const container = (0, import_react6.useRef)(void 0);
  const element = (0, import_react6.useRef)(void 0);
  const elementWidth = (0, import_react6.useRef)(void 0);
  const elementHeight = (0, import_react6.useRef)(void 0);
  const scrollContainer = (0, import_react6.useRef)(void 0);
  const fit = (0, import_react6.useCallback)(() => {
    if (!scrollContainer.current || !container.current || !element.current) {
      return;
    }
    const currentElementWidth = element.current.clientWidth;
    const currentElementHeight = element.current.clientHeight;
    if (elementWidth.current === currentElementWidth && elementHeight.current === currentElementHeight) {
      return;
    }
    elementWidth.current = currentElementWidth;
    elementHeight.current = currentElementHeight;
    const parent = container.current.parentElement;
    if (!parent) {
      return;
    }
    const style = window.getComputedStyle(element.current);
    const { position } = style;
    if (position !== "absolute") {
      element.current.style.position = "absolute";
    }
    const parentStyle = window.getComputedStyle(parent);
    const { position: parentPosition } = parentStyle;
    if (parentPosition !== "relative" && parentPosition !== "absolute") {
      parent.style.position = "relative";
    }
    alignBothAxis({
      axis: mainAxis,
      container: container.current,
      element: element.current,
      invertAxis,
      invertSecondaryAxis,
      scrollContainer: scrollContainer.current,
      spacing
    });
  }, [invertAxis, invertSecondaryAxis, mainAxis, spacing]);
  const child = import_react6.Children.only(children);
  (0, import_react6.useEffect)(() => {
    fit();
    function onMutation() {
      fit();
    }
    if (isMutationObserverSupported && element.current) {
      const mutationObserver = new MutationObserver(onMutation);
      mutationObserver.observe(element.current, {
        attributes: true,
        attributeFilter: ["class", "style"]
      });
    }
  }, [fit]);
  function assignRefs(domElement) {
    if (!domElement || !(domElement instanceof HTMLElement)) {
      return;
    }
    element.current = domElement;
    scrollContainer.current = findScrollContainer(domElement);
  }
  return (0, import_jsx_runtime25.jsx)("span", { ref: (domContainer) => {
    if (!domContainer) {
      return;
    }
    container.current = domContainer;
    const domElement = domContainer === null || domContainer === void 0 ? void 0 : domContainer.firstElementChild;
    assignRefs(domElement);
  }, style: { display: "contents" }, children: child });
}

// node_modules/react-fit/dist/index.js
var dist_default4 = Fit;

// node_modules/react-datetime-picker/dist/DateTimeInput.js
var import_jsx_runtime39 = __toESM(require_jsx_runtime(), 1);
var import_react9 = __toESM(require_react(), 1);

// node_modules/react-date-picker/dist/DateInput/DayInput.js
var import_jsx_runtime27 = __toESM(require_jsx_runtime());

// node_modules/react-date-picker/dist/DateInput/Input.js
var import_jsx_runtime26 = __toESM(require_jsx_runtime(), 1);
var import_react7 = __toESM(require_react(), 1);

// node_modules/update-input-width/dist/esm/index.js
var allowedVariants = ["normal", "small-caps"];
function getFontShorthand(element) {
  if (!element) {
    return "";
  }
  var style = window.getComputedStyle(element);
  if (style.font) {
    return style.font;
  }
  var isFontDefined = style.fontFamily !== "";
  if (!isFontDefined) {
    return "";
  }
  var fontVariant = allowedVariants.includes(style.fontVariant) ? style.fontVariant : "normal";
  return "".concat(style.fontStyle, " ").concat(fontVariant, " ").concat(style.fontWeight, " ").concat(style.fontSize, " / ").concat(style.lineHeight, " ").concat(style.fontFamily);
}
var cachedCanvas;
function measureText(text, font) {
  var canvas = cachedCanvas || (cachedCanvas = document.createElement("canvas"));
  var context = canvas.getContext("2d");
  if (!context) {
    return null;
  }
  context.font = font;
  var width = context.measureText(text).width;
  return Math.ceil(width);
}
function updateInputWidth(element) {
  if (typeof document === "undefined" || !element) {
    return null;
  }
  var font = getFontShorthand(element);
  var text = element.value || element.placeholder;
  var width = measureText(text, font);
  if (width === null) {
    return null;
  }
  element.style.width = "".concat(width, "px");
  return width;
}
var esm_default = updateInputWidth;

// node_modules/react-date-picker/dist/DateInput/Input.js
var isBrowser2 = typeof window !== "undefined";
var useIsomorphicLayoutEffect = isBrowser2 ? import_react7.useLayoutEffect : import_react7.useEffect;
var isIEOrEdgeLegacy = isBrowser2 && /(MSIE|Trident\/|Edge\/)/.test(navigator.userAgent);
var isFirefox = isBrowser2 && /Firefox/.test(navigator.userAgent);
function onFocus(event) {
  const { target } = event;
  if (isIEOrEdgeLegacy) {
    requestAnimationFrame(() => target.select());
  } else {
    target.select();
  }
}
function updateInputWidthOnLoad(element) {
  if (document.readyState === "complete") {
    return;
  }
  function onLoad() {
    esm_default(element);
  }
  window.addEventListener("load", onLoad);
}
function updateInputWidthOnFontLoad(element) {
  if (!document.fonts) {
    return;
  }
  const font = getFontShorthand(element);
  if (!font) {
    return;
  }
  const isFontLoaded = document.fonts.check(font);
  if (isFontLoaded) {
    return;
  }
  function onLoadingDone() {
    esm_default(element);
  }
  document.fonts.addEventListener("loadingdone", onLoadingDone);
}
function getSelectionString(input) {
  if (input && "selectionStart" in input && input.selectionStart !== null && "selectionEnd" in input && input.selectionEnd !== null) {
    return input.value.slice(input.selectionStart, input.selectionEnd);
  }
  if ("getSelection" in window) {
    const selection = window.getSelection();
    return selection === null || selection === void 0 ? void 0 : selection.toString();
  }
  return null;
}
function makeOnKeyPress(maxLength) {
  if (maxLength === null) {
    return void 0;
  }
  return function onKeyPress(event) {
    if (isFirefox) {
      return;
    }
    const { key, target: input } = event;
    const { value } = input;
    const isNumberKey = key.length === 1 && /\d/.test(key);
    const selection = getSelectionString(input);
    if (!isNumberKey || !(selection || value.length < maxLength)) {
      event.preventDefault();
    }
  };
}
function Input({ ariaLabel, autoFocus, className: className8, disabled, inputRef, max, min, name, nameForClass, onChange, onKeyDown, onKeyUp, placeholder = "--", required, showLeadingZeros, step, value }) {
  useIsomorphicLayoutEffect(() => {
    if (!inputRef || !inputRef.current) {
      return;
    }
    esm_default(inputRef.current);
    updateInputWidthOnLoad(inputRef.current);
    updateInputWidthOnFontLoad(inputRef.current);
  }, [inputRef, value]);
  const hasLeadingZero = showLeadingZeros && value && Number(value) < 10 && (value === "0" || !value.toString().startsWith("0"));
  const maxLength = max ? max.toString().length : null;
  return (0, import_jsx_runtime26.jsxs)(import_jsx_runtime26.Fragment, { children: [hasLeadingZero ? (0, import_jsx_runtime26.jsx)("span", { className: `${className8}__leadingZero`, children: "0" }) : null, (0, import_jsx_runtime26.jsx)("input", {
    "aria-label": ariaLabel,
    autoComplete: "off",
    // biome-ignore lint/a11y/noAutofocus: This is up to developers' decision
    autoFocus,
    className: clsx_default(`${className8}__input`, `${className8}__${nameForClass || name}`, hasLeadingZero && `${className8}__input--hasLeadingZero`),
    "data-input": "true",
    disabled,
    inputMode: "numeric",
    max,
    min,
    name,
    onChange,
    onFocus,
    onKeyDown,
    onKeyPress: makeOnKeyPress(maxLength),
    onKeyUp: (event) => {
      esm_default(event.target);
      if (onKeyUp) {
        onKeyUp(event);
      }
    },
    placeholder,
    // Assertion is needed for React 18 compatibility
    ref: inputRef,
    required,
    step,
    type: "number",
    value: value !== null ? value : ""
  })] });
}

// node_modules/react-date-picker/dist/shared/utils.js
function isValidNumber2(num) {
  return num !== null && num !== false && !Number.isNaN(Number(num));
}
function safeMin(...args) {
  return Math.min(...args.filter(isValidNumber2));
}
function safeMax2(...args) {
  return Math.max(...args.filter(isValidNumber2));
}

// node_modules/react-date-picker/dist/DateInput/DayInput.js
function DayInput({ maxDate, minDate, month, year, ...otherProps }) {
  const currentMonthMaxDays = (() => {
    if (!month) {
      return 31;
    }
    return getDaysInMonth(new Date(Number(year), Number(month) - 1, 1));
  })();
  function isSameMonth(date) {
    return year === getYear(date).toString() && month === getMonthHuman(date).toString();
  }
  const maxDay = safeMin(currentMonthMaxDays, maxDate && isSameMonth(maxDate) && getDate(maxDate));
  const minDay = safeMax2(1, minDate && isSameMonth(minDate) && getDate(minDate));
  return (0, import_jsx_runtime27.jsx)(Input, { max: maxDay, min: minDay, name: "day", ...otherProps });
}

// node_modules/react-date-picker/dist/DateInput/MonthInput.js
var import_jsx_runtime28 = __toESM(require_jsx_runtime());
function MonthInput({ maxDate, minDate, year, ...otherProps }) {
  function isSameYear(date) {
    return date && year === getYear(date).toString();
  }
  const maxMonth = safeMin(12, maxDate && isSameYear(maxDate) && getMonthHuman(maxDate));
  const minMonth = safeMax2(1, minDate && isSameYear(minDate) && getMonthHuman(minDate));
  return (0, import_jsx_runtime28.jsx)(Input, { max: maxMonth, min: minMonth, name: "month", ...otherProps });
}

// node_modules/react-date-picker/dist/DateInput/MonthSelect.js
var import_jsx_runtime29 = __toESM(require_jsx_runtime());

// node_modules/react-date-picker/dist/shared/dateFormatter.js
var formatterCache2 = /* @__PURE__ */ new Map();
function getFormatter2(options) {
  return function formatter(locale, date) {
    const localeWithDefault = locale || dist_default();
    if (!formatterCache2.has(localeWithDefault)) {
      formatterCache2.set(localeWithDefault, /* @__PURE__ */ new Map());
    }
    const formatterCacheLocale = formatterCache2.get(localeWithDefault);
    if (!formatterCacheLocale.has(options)) {
      const formatter2 = new Intl.DateTimeFormat(localeWithDefault || void 0, options);
      formatterCacheLocale.set(options, formatter2.format.bind(formatter2));
    }
    return formatterCacheLocale.get(options)(date);
  };
}
function toSafeHour2(date) {
  const safeDate = new Date(date);
  return new Date(safeDate.setHours(12));
}
function getSafeFormatter2(options) {
  return (locale, date) => getFormatter2(options)(locale, toSafeHour2(date));
}
var formatMonthOptions2 = { month: "long" };
var formatShortMonthOptions = { month: "short" };
var formatMonth2 = getSafeFormatter2(formatMonthOptions2);
var formatShortMonth = getSafeFormatter2(formatShortMonthOptions);

// node_modules/react-date-picker/dist/DateInput/MonthSelect.js
function MonthSelect({ ariaLabel, autoFocus, className: className8, disabled, inputRef, locale, maxDate, minDate, onChange, onKeyDown, placeholder = "--", required, short, value, year }) {
  function isSameYear(date) {
    return date && year === getYear(date).toString();
  }
  const maxMonth = safeMin(12, maxDate && isSameYear(maxDate) && getMonthHuman(maxDate));
  const minMonth = safeMax2(1, minDate && isSameYear(minDate) && getMonthHuman(minDate));
  const dates = [...Array(12)].map((_el, index) => new Date(2019, index, 1));
  const name = "month";
  const formatter = short ? formatShortMonth : formatMonth2;
  return (0, import_jsx_runtime29.jsxs)("select", {
    "aria-label": ariaLabel,
    // biome-ignore lint/a11y/noAutofocus: This is up to developers' decision
    autoFocus,
    className: clsx_default(`${className8}__input`, `${className8}__${name}`),
    "data-input": "true",
    "data-select": "true",
    disabled,
    name,
    onChange,
    onKeyDown,
    // Assertion is needed for React 18 compatibility
    ref: inputRef,
    required,
    value: value !== null ? value : "",
    children: [!value && (0, import_jsx_runtime29.jsx)("option", { value: "", children: placeholder }), dates.map((date) => {
      const month = getMonthHuman(date);
      const disabled2 = month < minMonth || month > maxMonth;
      return (0, import_jsx_runtime29.jsx)("option", { disabled: disabled2, value: month, children: formatter(locale, date) }, month);
    })]
  });
}

// node_modules/react-date-picker/dist/DateInput/YearInput.js
var import_jsx_runtime30 = __toESM(require_jsx_runtime());
function YearInput({ maxDate, minDate, placeholder = "----", valueType, ...otherProps }) {
  const maxYear = safeMin(275760, maxDate && getYear(maxDate));
  const minYear = safeMax2(1, minDate && getYear(minDate));
  const yearStep = (() => {
    if (valueType === "century") {
      return 10;
    }
    return 1;
  })();
  return (0, import_jsx_runtime30.jsx)(Input, { max: maxYear, min: minYear, name: "year", placeholder, step: yearStep, ...otherProps });
}

// node_modules/react-time-picker/dist/TimeInput/AmPm.js
var import_jsx_runtime31 = __toESM(require_jsx_runtime());

// node_modules/react-time-picker/dist/shared/dates.js
function convert24to12(hour24) {
  const hour12 = Number(hour24) % 12 || 12;
  return [hour12, Number(hour24) < 12 ? "am" : "pm"];
}

// node_modules/react-time-picker/dist/shared/dateFormatter.js
var formatterCache3 = /* @__PURE__ */ new Map();
function getFormatter3(options) {
  return function formatter(locale, date) {
    const localeWithDefault = locale || dist_default();
    if (!formatterCache3.has(localeWithDefault)) {
      formatterCache3.set(localeWithDefault, /* @__PURE__ */ new Map());
    }
    const formatterCacheLocale = formatterCache3.get(localeWithDefault);
    if (!formatterCacheLocale.has(options)) {
      const formatter2 = new Intl.DateTimeFormat(localeWithDefault || void 0, options);
      formatterCacheLocale.set(options, formatter2.format.bind(formatter2));
    }
    return formatterCacheLocale.get(options)(date);
  };
}

// node_modules/react-time-picker/dist/shared/utils.js
var nines = ["9", "٩"];
var ninesRegExp = new RegExp(`[${nines.join("")}]`);
var amPmFormatter = getFormatter3({ hour: "numeric" });
function getAmPmLabels(locale) {
  const amString = amPmFormatter(locale, new Date(2017, 0, 1, 9));
  const pmString = amPmFormatter(locale, new Date(2017, 0, 1, 21));
  const [am1, am2] = amString.split(ninesRegExp);
  const [pm1, pm2] = pmString.split(ninesRegExp);
  if (pm2 !== void 0) {
    if (am1 !== pm1) {
      return [am1, pm1].map((el) => el.trim());
    }
    if (am2 !== pm2) {
      return [am2, pm2].map((el) => el.trim());
    }
  }
  return ["AM", "PM"];
}
function isValidNumber3(num) {
  return num !== null && num !== false && !Number.isNaN(Number(num));
}
function safeMin2(...args) {
  return Math.min(...args.filter(isValidNumber3));
}
function safeMax3(...args) {
  return Math.max(...args.filter(isValidNumber3));
}

// node_modules/react-time-picker/dist/TimeInput/AmPm.js
function AmPm({ ariaLabel, autoFocus, className: className8, disabled, inputRef, locale, maxTime, minTime, onChange, onKeyDown, required, value }) {
  const amDisabled = minTime ? convert24to12(getHours(minTime))[1] === "pm" : false;
  const pmDisabled = maxTime ? convert24to12(getHours(maxTime))[1] === "am" : false;
  const name = "amPm";
  const [amLabel, pmLabel] = getAmPmLabels(locale);
  return (0, import_jsx_runtime31.jsxs)("select", {
    "aria-label": ariaLabel,
    // biome-ignore lint/a11y/noAutofocus: This is up to developers' decision
    autoFocus,
    className: clsx_default(`${className8}__input`, `${className8}__${name}`),
    "data-input": "true",
    "data-select": "true",
    disabled,
    name,
    onChange,
    onKeyDown,
    // Assertion is needed for React 18 compatibility
    ref: inputRef,
    required,
    value: value !== null ? value : "",
    children: [!value && (0, import_jsx_runtime31.jsx)("option", { value: "", children: "--" }), (0, import_jsx_runtime31.jsx)("option", { disabled: amDisabled, value: "am", children: amLabel }), (0, import_jsx_runtime31.jsx)("option", { disabled: pmDisabled, value: "pm", children: pmLabel })]
  });
}

// node_modules/react-time-picker/dist/TimeInput/Hour12Input.js
var import_jsx_runtime33 = __toESM(require_jsx_runtime());

// node_modules/react-time-picker/dist/TimeInput/Input.js
var import_jsx_runtime32 = __toESM(require_jsx_runtime(), 1);
var import_react8 = __toESM(require_react(), 1);
var isBrowser3 = typeof window !== "undefined";
var useIsomorphicLayoutEffect2 = isBrowser3 ? import_react8.useLayoutEffect : import_react8.useEffect;
var isIEOrEdgeLegacy2 = isBrowser3 && /(MSIE|Trident\/|Edge\/)/.test(navigator.userAgent);
var isFirefox2 = isBrowser3 && /Firefox/.test(navigator.userAgent);
function onFocus2(event) {
  const { target } = event;
  if (isIEOrEdgeLegacy2) {
    requestAnimationFrame(() => target.select());
  } else {
    target.select();
  }
}
function updateInputWidthOnLoad2(element) {
  if (document.readyState === "complete") {
    return;
  }
  function onLoad() {
    esm_default(element);
  }
  window.addEventListener("load", onLoad);
}
function updateInputWidthOnFontLoad2(element) {
  if (!document.fonts) {
    return;
  }
  const font = getFontShorthand(element);
  if (!font) {
    return;
  }
  const isFontLoaded = document.fonts.check(font);
  if (isFontLoaded) {
    return;
  }
  function onLoadingDone() {
    esm_default(element);
  }
  document.fonts.addEventListener("loadingdone", onLoadingDone);
}
function getSelectionString2(input) {
  if (input && "selectionStart" in input && input.selectionStart !== null && "selectionEnd" in input && input.selectionEnd !== null) {
    return input.value.slice(input.selectionStart, input.selectionEnd);
  }
  if ("getSelection" in window) {
    const selection = window.getSelection();
    return selection === null || selection === void 0 ? void 0 : selection.toString();
  }
  return null;
}
function makeOnKeyPress2(maxLength) {
  if (maxLength === null) {
    return void 0;
  }
  return function onKeyPress(event) {
    if (isFirefox2) {
      return;
    }
    const { key, target: input } = event;
    const { value } = input;
    const isNumberKey = key.length === 1 && /\d/.test(key);
    const selection = getSelectionString2(input);
    if (!isNumberKey || !(selection || value.length < maxLength)) {
      event.preventDefault();
    }
  };
}
function Input2({ ariaLabel, autoFocus, className: className8, disabled, inputRef, max, min, name, nameForClass, onChange, onKeyDown, onKeyUp, placeholder = "--", required, showLeadingZeros, step, value }) {
  useIsomorphicLayoutEffect2(() => {
    if (!inputRef || !inputRef.current) {
      return;
    }
    esm_default(inputRef.current);
    updateInputWidthOnLoad2(inputRef.current);
    updateInputWidthOnFontLoad2(inputRef.current);
  }, [inputRef, value]);
  const hasLeadingZero = showLeadingZeros && value && Number(value) < 10 && (value === "0" || !value.toString().startsWith("0"));
  const maxLength = max ? max.toString().length : null;
  return (0, import_jsx_runtime32.jsxs)(import_jsx_runtime32.Fragment, { children: [hasLeadingZero ? (0, import_jsx_runtime32.jsx)("span", { className: `${className8}__leadingZero`, children: "0" }) : null, (0, import_jsx_runtime32.jsx)("input", {
    "aria-label": ariaLabel,
    autoComplete: "off",
    // biome-ignore lint/a11y/noAutofocus: This is up to developers' decision
    autoFocus,
    className: clsx_default(`${className8}__input`, `${className8}__${nameForClass || name}`, hasLeadingZero && `${className8}__input--hasLeadingZero`),
    "data-input": "true",
    disabled,
    inputMode: "numeric",
    max,
    min,
    name,
    onChange,
    onFocus: onFocus2,
    onKeyDown,
    onKeyPress: makeOnKeyPress2(maxLength),
    onKeyUp: (event) => {
      esm_default(event.target);
      if (onKeyUp) {
        onKeyUp(event);
      }
    },
    placeholder,
    // Assertion is needed for React 18 compatibility
    ref: inputRef,
    required,
    step,
    type: "number",
    value: value !== null ? value : ""
  })] });
}

// node_modules/react-time-picker/dist/TimeInput/Hour12Input.js
function Hour12Input({ amPm, maxTime, minTime, value, ...otherProps }) {
  const maxHour = safeMin2(12, maxTime && (() => {
    const [maxHourResult, maxAmPm] = convert24to12(getHours(maxTime));
    if (maxAmPm !== amPm) {
      return null;
    }
    return maxHourResult;
  })());
  const minHour = safeMax3(1, minTime && (() => {
    const [minHourResult, minAmPm] = convert24to12(getHours(minTime));
    if (
      // pm is always after am, so we should ignore validation
      minAmPm !== amPm || // If minHour is 12 am/pm, user should be able to enter 12, 1, ..., 11.
      minHourResult === 12
    ) {
      return null;
    }
    return minHourResult;
  })());
  const value12 = value ? convert24to12(value)[0].toString() : "";
  return (0, import_jsx_runtime33.jsx)(Input2, { max: maxHour, min: minHour, name: "hour12", nameForClass: "hour", value: value12, ...otherProps });
}

// node_modules/react-time-picker/dist/TimeInput/Hour24Input.js
var import_jsx_runtime34 = __toESM(require_jsx_runtime());
function Hour24Input({ maxTime, minTime, ...otherProps }) {
  const maxHour = safeMin2(23, maxTime && getHours(maxTime));
  const minHour = safeMax3(0, minTime && getHours(minTime));
  return (0, import_jsx_runtime34.jsx)(Input2, { max: maxHour, min: minHour, name: "hour24", nameForClass: "hour", ...otherProps });
}

// node_modules/react-time-picker/dist/TimeInput/MinuteInput.js
var import_jsx_runtime35 = __toESM(require_jsx_runtime());
function MinuteInput({ hour, maxTime, minTime, showLeadingZeros = true, ...otherProps }) {
  function isSameHour(date) {
    return hour === getHours(date).toString();
  }
  const maxMinute = safeMin2(59, maxTime && isSameHour(maxTime) && getMinutes(maxTime));
  const minMinute = safeMax3(0, minTime && isSameHour(minTime) && getMinutes(minTime));
  return (0, import_jsx_runtime35.jsx)(Input2, { max: maxMinute, min: minMinute, name: "minute", showLeadingZeros, ...otherProps });
}

// node_modules/react-time-picker/dist/TimeInput/SecondInput.js
var import_jsx_runtime36 = __toESM(require_jsx_runtime());
function SecondInput({ hour, maxTime, minTime, minute, showLeadingZeros = true, ...otherProps }) {
  function isSameMinute(date) {
    return hour === getHours(date).toString() && minute === getMinutes(date).toString();
  }
  const maxSecond = safeMin2(59, maxTime && isSameMinute(maxTime) && getSeconds(maxTime));
  const minSecond = safeMax3(0, minTime && isSameMinute(minTime) && getSeconds(minTime));
  return (0, import_jsx_runtime36.jsx)(Input2, { max: maxSecond, min: minSecond, name: "second", showLeadingZeros, ...otherProps });
}

// node_modules/react-datetime-picker/dist/DateTimeInput/NativeInput.js
var import_jsx_runtime37 = __toESM(require_jsx_runtime(), 1);
function NativeInput({ ariaLabel, disabled, maxDate, minDate, name, onChange, required, value, valueType }) {
  const nativeValueParser = (() => {
    switch (valueType) {
      case "hour":
        return (receivedValue) => `${getISOLocalDate(receivedValue)}T${getHours(receivedValue)}:00`;
      case "minute":
        return (receivedValue) => `${getISOLocalDate(receivedValue)}T${getHoursMinutes(receivedValue)}`;
      case "second":
        return getISOLocalDateTime;
      default:
        throw new Error("Invalid valueType");
    }
  })();
  const step = (() => {
    switch (valueType) {
      case "hour":
        return 3600;
      case "minute":
        return 60;
      case "second":
        return 1;
      default:
        throw new Error("Invalid valueType");
    }
  })();
  function stopPropagation(event) {
    event.stopPropagation();
  }
  return (0, import_jsx_runtime37.jsx)("input", { "aria-label": ariaLabel, disabled, hidden: true, max: maxDate ? nativeValueParser(maxDate) : void 0, min: minDate ? nativeValueParser(minDate) : void 0, name, onChange, onFocus: stopPropagation, required, step, style: {
    visibility: "hidden",
    position: "absolute",
    zIndex: "-999"
  }, type: "datetime-local", value: value ? nativeValueParser(value) : "" });
}

// node_modules/react-datetime-picker/dist/Divider.js
var import_jsx_runtime38 = __toESM(require_jsx_runtime(), 1);
function Divider({ children }) {
  return (0, import_jsx_runtime38.jsx)("span", { className: "react-datetime-picker__inputGroup__divider", "data-testid": "divider", children });
}

// node_modules/react-datetime-picker/dist/shared/dateFormatter.js
var formatterCache4 = /* @__PURE__ */ new Map();
function getFormatter4(options) {
  return function formatter(locale, date) {
    const localeWithDefault = locale || dist_default();
    if (!formatterCache4.has(localeWithDefault)) {
      formatterCache4.set(localeWithDefault, /* @__PURE__ */ new Map());
    }
    const formatterCacheLocale = formatterCache4.get(localeWithDefault);
    if (!formatterCacheLocale.has(options)) {
      const formatter2 = new Intl.DateTimeFormat(localeWithDefault || void 0, options);
      formatterCacheLocale.set(options, formatter2.format.bind(formatter2));
    }
    return formatterCacheLocale.get(options)(date);
  };
}
var numberFormatterCache = /* @__PURE__ */ new Map();
function getNumberFormatter(options) {
  return (locale, number) => {
    const localeWithDefault = locale || dist_default();
    if (!numberFormatterCache.has(localeWithDefault)) {
      numberFormatterCache.set(localeWithDefault, /* @__PURE__ */ new Map());
    }
    const numberFormatterCacheLocale = numberFormatterCache.get(localeWithDefault);
    if (!numberFormatterCacheLocale.has(options)) {
      const formatter = new Intl.NumberFormat(localeWithDefault || void 0, options);
      numberFormatterCacheLocale.set(options, formatter.format.bind(formatter));
    }
    return numberFormatterCacheLocale.get(options)(number);
  };
}
var formatDateOptions2 = {
  day: "numeric",
  month: "numeric",
  year: "numeric"
};
var formatDate2 = getFormatter4(formatDateOptions2);

// node_modules/react-datetime-picker/dist/shared/dates.js
function convert12to24(hour12, amPm) {
  let hour24 = Number(hour12);
  if (amPm === "am" && hour24 === 12) {
    hour24 = 0;
  } else if (amPm === "pm" && hour24 < 12) {
    hour24 += 12;
  }
  return hour24;
}
function convert24to122(hour24) {
  const hour12 = Number(hour24) % 12 || 12;
  return [hour12, Number(hour24) < 12 ? "am" : "pm"];
}

// node_modules/react-datetime-picker/dist/shared/utils.js
function between2(value, min, max) {
  if (min && min > value) {
    return min;
  }
  if (max && max < value) {
    return max;
  }
  return value;
}
var nines2 = ["9", "٩"];
var ninesRegExp2 = new RegExp(`[${nines2.join("")}]`);
var amPmFormatter2 = getFormatter4({ hour: "numeric" });
function getAmPmLabels2(locale) {
  const amString = amPmFormatter2(locale, new Date(2017, 0, 1, 9));
  const pmString = amPmFormatter2(locale, new Date(2017, 0, 1, 21));
  const [am1, am2] = amString.split(ninesRegExp2);
  const [pm1, pm2] = pmString.split(ninesRegExp2);
  if (pm2 !== void 0) {
    if (am1 !== pm1) {
      return [am1, pm1].map((el) => el.trim());
    }
    if (am2 !== pm2) {
      return [am2, pm2].map((el) => el.trim());
    }
  }
  return ["AM", "PM"];
}

// node_modules/react-datetime-picker/dist/DateTimeInput.js
var getFormatterOptionsCache = {};
var defaultMinDate2 = /* @__PURE__ */ new Date();
defaultMinDate2.setFullYear(1, 0, 1);
defaultMinDate2.setHours(0, 0, 0, 0);
var defaultMaxDate2 = /* @__PURE__ */ new Date(864e13);
var allViews2 = ["hour", "minute", "second"];
function toDate2(value) {
  if (value instanceof Date) {
    return value;
  }
  return new Date(value);
}
function isSameDate(date, year, month, day) {
  return year === getYear(date).toString() && month === getMonthHuman(date).toString() && day === getDate(date).toString();
}
function getValue2(value, index) {
  const rawValue = Array.isArray(value) ? value[index] : value;
  if (!rawValue) {
    return null;
  }
  const valueDate = toDate2(rawValue);
  if (Number.isNaN(valueDate.getTime())) {
    throw new Error(`Invalid date: ${value}`);
  }
  return valueDate;
}
function getDetailValue2({ value, minDate, maxDate }, index) {
  const valuePiece = getValue2(value, index);
  if (!valuePiece) {
    return null;
  }
  return between2(valuePiece, minDate, maxDate);
}
var getDetailValueFrom2 = (args) => getDetailValue2(args, 0);
function isInternalInput(element) {
  return element.dataset.input === "true";
}
function findInput(element, property) {
  let nextElement = element;
  do {
    nextElement = nextElement[property];
  } while (nextElement && !isInternalInput(nextElement));
  return nextElement;
}
function focus(element) {
  if (element) {
    element.focus();
  }
}
function renderCustomInputs(placeholder, elementFunctions, allowMultipleInstances) {
  const usedFunctions = [];
  const pattern = new RegExp(Object.keys(elementFunctions).map((el) => `${el}+`).join("|"), "g");
  const matches = placeholder.match(pattern);
  return placeholder.split(pattern).reduce((arr, element, index) => {
    const divider = element && (0, import_jsx_runtime39.jsx)(Divider, { children: element }, `separator_${index}`);
    arr.push(divider);
    const currentMatch = matches === null || matches === void 0 ? void 0 : matches[index];
    if (currentMatch) {
      const renderFunction = elementFunctions[currentMatch] || elementFunctions[Object.keys(elementFunctions).find((elementFunction) => currentMatch.match(elementFunction))];
      if (!renderFunction) {
        return arr;
      }
      if (!allowMultipleInstances && usedFunctions.includes(renderFunction)) {
        arr.push(currentMatch);
      } else {
        arr.push(renderFunction(currentMatch, index));
        usedFunctions.push(renderFunction);
      }
    }
    return arr;
  }, []);
}
var formatNumber = getNumberFormatter({ useGrouping: false });
function DateTimeInput({ amPmAriaLabel, autoFocus, className: className8, dayAriaLabel, dayPlaceholder, disabled, format, hourAriaLabel, hourPlaceholder, isWidgetOpen: isWidgetOpenProps, locale, maxDate, maxDetail = "minute", minDate, minuteAriaLabel, minutePlaceholder, monthAriaLabel, monthPlaceholder, name = "datetime", nativeInputAriaLabel, onChange: onChangeProps, onInvalidChange, required, secondAriaLabel, secondPlaceholder, showLeadingZeros, value: valueProps, yearAriaLabel, yearPlaceholder }) {
  const [amPm, setAmPm] = (0, import_react9.useState)(null);
  const [year, setYear] = (0, import_react9.useState)(null);
  const [month, setMonth] = (0, import_react9.useState)(null);
  const [day, setDay] = (0, import_react9.useState)(null);
  const [hour, setHour] = (0, import_react9.useState)(null);
  const [minute, setMinute] = (0, import_react9.useState)(null);
  const [second, setSecond] = (0, import_react9.useState)(null);
  const [value, setValue] = (0, import_react9.useState)(null);
  const amPmInput = (0, import_react9.useRef)(null);
  const yearInput = (0, import_react9.useRef)(null);
  const monthInput = (0, import_react9.useRef)(null);
  const monthSelect = (0, import_react9.useRef)(null);
  const dayInput = (0, import_react9.useRef)(null);
  const hour12Input = (0, import_react9.useRef)(null);
  const hour24Input = (0, import_react9.useRef)(null);
  const minuteInput = (0, import_react9.useRef)(null);
  const secondInput = (0, import_react9.useRef)(null);
  const [isWidgetOpen, setIsWidgetOpenOpen] = (0, import_react9.useState)(isWidgetOpenProps);
  const lastPressedKey = (0, import_react9.useRef)(void 0);
  (0, import_react9.useEffect)(() => {
    setIsWidgetOpenOpen(isWidgetOpenProps);
  }, [isWidgetOpenProps]);
  (0, import_react9.useEffect)(() => {
    const nextValue = getDetailValueFrom2({
      value: valueProps,
      minDate,
      maxDate
    });
    if (nextValue) {
      setAmPm(convert24to122(getHours(nextValue))[1]);
      setYear(getYear(nextValue).toString());
      setMonth(getMonthHuman(nextValue).toString());
      setDay(getDate(nextValue).toString());
      setHour(getHours(nextValue).toString());
      setMinute(getMinutes(nextValue).toString());
      setSecond(getSeconds(nextValue).toString());
      setValue(toDate2(nextValue));
    } else {
      setAmPm(null);
      setYear(null);
      setMonth(null);
      setDay(null);
      setHour(null);
      setMinute(null);
      setSecond(null);
      setValue(null);
    }
  }, [
    valueProps,
    minDate,
    maxDate,
    // Toggling widget visibility resets values
    isWidgetOpen
  ]);
  const valueType = maxDetail;
  const formatTime = (() => {
    const level = allViews2.indexOf(maxDetail);
    const formatterOptions = getFormatterOptionsCache[level] || (() => {
      const options = { hour: "numeric" };
      if (level >= 1) {
        options.minute = "numeric";
      }
      if (level >= 2) {
        options.second = "numeric";
      }
      getFormatterOptionsCache[level] = options;
      return options;
    })();
    return getFormatter4(formatterOptions);
  })();
  const datePlaceholder = (() => {
    const year2 = 2017;
    const monthIndex = 11;
    const day2 = 11;
    const date = new Date(year2, monthIndex, day2);
    const formattedDate = formatDate2(locale, date);
    const datePieces = ["year", "month", "day"];
    const datePieceReplacements = ["y", "M", "d"];
    function formatDatePiece(name2, dateToFormat) {
      const formatterOptions = getFormatterOptionsCache[name2] || (() => {
        const options = { [name2]: "numeric" };
        getFormatterOptionsCache[name2] = options;
        return options;
      })();
      return getFormatter4(formatterOptions)(locale, dateToFormat).match(/\d{1,}/);
    }
    let placeholder2 = formattedDate;
    datePieces.forEach((datePiece, index) => {
      const match = formatDatePiece(datePiece, date);
      if (match) {
        const formattedDatePiece = match[0];
        const datePieceReplacement = datePieceReplacements[index];
        placeholder2 = placeholder2.replace(formattedDatePiece, datePieceReplacement);
      }
    });
    placeholder2 = placeholder2.replace("17", "y");
    return placeholder2;
  })();
  const timePlaceholder = (() => {
    const hour24 = 21;
    const hour12 = 9;
    const minute2 = 13;
    const second2 = 14;
    const date = new Date(2017, 0, 1, hour24, minute2, second2);
    return formatTime(locale, date).replace(formatNumber(locale, hour12), "h").replace(formatNumber(locale, hour24), "H").replace(formatNumber(locale, minute2), "mm").replace(formatNumber(locale, second2), "ss").replace(new RegExp(getAmPmLabels2(locale).join("|")), "a");
  })();
  const placeholder = format || `${datePlaceholder} ${timePlaceholder}`;
  const dateDivider = (() => {
    const dividers = datePlaceholder.match(/[^0-9a-z]/i);
    return dividers ? dividers[0] : null;
  })();
  const timeDivider = (() => {
    const dividers = timePlaceholder.match(/[^0-9a-z]/i);
    return dividers ? dividers[0] : null;
  })();
  const maxTime = (() => {
    if (!maxDate) {
      return void 0;
    }
    if (!isSameDate(maxDate, year, month, day)) {
      return void 0;
    }
    return getHoursMinutesSeconds(maxDate || defaultMaxDate2);
  })();
  const minTime = (() => {
    if (!minDate) {
      return void 0;
    }
    if (!isSameDate(minDate, year, month, day)) {
      return void 0;
    }
    return getHoursMinutesSeconds(minDate || defaultMinDate2);
  })();
  function onClick(event) {
    if (event.target === event.currentTarget) {
      const firstInput = event.target.children[1];
      focus(firstInput);
    }
  }
  function onKeyDown(event) {
    lastPressedKey.current = event.key;
    switch (event.key) {
      case "ArrowLeft":
      case "ArrowRight":
      case dateDivider:
      case timeDivider: {
        event.preventDefault();
        const { target: input } = event;
        const property = event.key === "ArrowLeft" ? "previousElementSibling" : "nextElementSibling";
        const nextInput = findInput(input, property);
        focus(nextInput);
        break;
      }
      default:
    }
  }
  function onKeyUp(event) {
    const { key, target: input } = event;
    const isLastPressedKey = lastPressedKey.current === key;
    if (!isLastPressedKey) {
      return;
    }
    const isNumberKey = !Number.isNaN(Number(key));
    if (!isNumberKey) {
      return;
    }
    const max = input.getAttribute("max");
    if (!max) {
      return;
    }
    const { value: value2 } = input;
    if (Number(value2) * 10 > Number(max) || value2.length >= max.length) {
      const property = "nextElementSibling";
      const nextInput = findInput(input, property);
      focus(nextInput);
    }
  }
  function onChangeExternal() {
    if (!onChangeProps) {
      return;
    }
    function filterBoolean(value2) {
      return Boolean(value2);
    }
    const formElements = [
      amPmInput.current,
      dayInput.current,
      monthInput.current,
      monthSelect.current,
      yearInput.current,
      hour12Input.current,
      hour24Input.current,
      minuteInput.current,
      secondInput.current
    ].filter(filterBoolean);
    const formElementsWithoutSelect = formElements.slice(1);
    const values = {};
    for (const formElement of formElements) {
      values[formElement.name] = formElement.type === "number" ? formElement.valueAsNumber : formElement.value;
    }
    const isEveryValueEmpty = formElementsWithoutSelect.every((formElement) => !formElement.value);
    if (isEveryValueEmpty) {
      onChangeProps(null, false);
      return;
    }
    const isEveryValueFilled = formElements.every((formElement) => formElement.value);
    const isEveryValueValid = formElements.every((formElement) => formElement.validity.valid);
    if (isEveryValueFilled && isEveryValueValid) {
      const year2 = Number(values.year || (/* @__PURE__ */ new Date()).getFullYear());
      const monthIndex = Number(values.month || 1) - 1;
      const day2 = Number(values.day || 1);
      const hour2 = Number(values.hour24 || values.hour12 && values.amPm && convert12to24(values.hour12, values.amPm) || 0);
      const minute2 = Number(values.minute || 0);
      const second2 = Number(values.second || 0);
      const proposedValue = /* @__PURE__ */ new Date();
      proposedValue.setFullYear(year2, monthIndex, day2);
      proposedValue.setHours(hour2, minute2, second2, 0);
      onChangeProps(proposedValue, false);
      return;
    }
    if (!onInvalidChange) {
      return;
    }
    onInvalidChange();
  }
  function onChange(event) {
    const { name: name2, value: value2 } = event.target;
    switch (name2) {
      case "amPm":
        setAmPm(value2);
        break;
      case "year":
        setYear(value2);
        break;
      case "month":
        setMonth(value2);
        break;
      case "day":
        setDay(value2);
        break;
      case "hour12":
        setHour(value2 ? convert12to24(value2, amPm || "am").toString() : "");
        break;
      case "hour24":
        setHour(value2);
        break;
      case "minute":
        setMinute(value2);
        break;
      case "second":
        setSecond(value2);
        break;
    }
    onChangeExternal();
  }
  function onChangeNative(event) {
    const { value: value2 } = event.target;
    if (!onChangeProps) {
      return;
    }
    const processedValue = (() => {
      if (!value2) {
        return null;
      }
      const [valueDate, valueTime] = value2.split("T");
      const [yearString, monthString, dayString] = valueDate.split("-");
      const year2 = Number(yearString);
      const monthIndex = Number(monthString) - 1 || 0;
      const day2 = Number(dayString) || 1;
      const [hourString, minuteString, secondString] = valueTime.split(":");
      const hour2 = Number(hourString) || 0;
      const minute2 = Number(minuteString) || 0;
      const second2 = Number(secondString) || 0;
      const proposedValue = /* @__PURE__ */ new Date();
      proposedValue.setFullYear(year2, monthIndex, day2);
      proposedValue.setHours(hour2, minute2, second2, 0);
      return proposedValue;
    })();
    onChangeProps(processedValue, false);
  }
  const commonInputProps = {
    className: className8,
    disabled,
    maxDate: maxDate || defaultMaxDate2,
    minDate: minDate || defaultMinDate2,
    onChange,
    onKeyDown,
    onKeyUp,
    // This is only for showing validity when editing
    required: Boolean(required || isWidgetOpen)
  };
  const commonTimeInputProps = {
    maxTime,
    minTime
  };
  function renderDay(currentMatch, index) {
    if (currentMatch && currentMatch.length > 2) {
      throw new Error(`Unsupported token: ${currentMatch}`);
    }
    const showLeadingZerosFromFormat = currentMatch && currentMatch.length === 2;
    return (0, import_jsx_runtime39.jsx)(DayInput, { ...commonInputProps, ariaLabel: dayAriaLabel, autoFocus: index === 0 && autoFocus, inputRef: dayInput, month, placeholder: dayPlaceholder, showLeadingZeros: showLeadingZerosFromFormat || showLeadingZeros, value: day, year }, "day");
  }
  function renderMonth(currentMatch, index) {
    if (currentMatch && currentMatch.length > 4) {
      throw new Error(`Unsupported token: ${currentMatch}`);
    }
    if (currentMatch.length > 2) {
      return (0, import_jsx_runtime39.jsx)(MonthSelect, { ...commonInputProps, ariaLabel: monthAriaLabel, autoFocus: index === 0 && autoFocus, inputRef: monthSelect, locale, placeholder: monthPlaceholder, short: currentMatch.length === 3, value: month, year }, "month");
    }
    const showLeadingZerosFromFormat = currentMatch && currentMatch.length === 2;
    return (0, import_jsx_runtime39.jsx)(MonthInput, { ...commonInputProps, ariaLabel: monthAriaLabel, autoFocus: index === 0 && autoFocus, inputRef: monthInput, placeholder: monthPlaceholder, showLeadingZeros: showLeadingZerosFromFormat || showLeadingZeros, value: month, year }, "month");
  }
  function renderYear(_currentMatch, index) {
    return (0, import_jsx_runtime39.jsx)(YearInput, { ...commonInputProps, ariaLabel: yearAriaLabel, autoFocus: index === 0 && autoFocus, inputRef: yearInput, placeholder: yearPlaceholder, value: year, valueType: "day" }, "year");
  }
  function renderHour12(currentMatch, index) {
    if (currentMatch && currentMatch.length > 2) {
      throw new Error(`Unsupported token: ${currentMatch}`);
    }
    const showLeadingZeros2 = currentMatch ? currentMatch.length === 2 : false;
    return (0, import_jsx_runtime39.jsx)(Hour12Input, { ...commonInputProps, ...commonTimeInputProps, amPm, ariaLabel: hourAriaLabel, autoFocus: index === 0 && autoFocus, inputRef: hour12Input, placeholder: hourPlaceholder, showLeadingZeros: showLeadingZeros2, value: hour }, "hour12");
  }
  function renderHour24(currentMatch, index) {
    if (currentMatch && currentMatch.length > 2) {
      throw new Error(`Unsupported token: ${currentMatch}`);
    }
    const showLeadingZeros2 = currentMatch ? currentMatch.length === 2 : false;
    return (0, import_jsx_runtime39.jsx)(Hour24Input, { ...commonInputProps, ...commonTimeInputProps, ariaLabel: hourAriaLabel, autoFocus: index === 0 && autoFocus, inputRef: hour24Input, placeholder: hourPlaceholder, showLeadingZeros: showLeadingZeros2, value: hour }, "hour24");
  }
  function renderHour(currentMatch, index) {
    if (/h/.test(currentMatch)) {
      return renderHour12(currentMatch, index);
    }
    return renderHour24(currentMatch, index);
  }
  function renderMinute(currentMatch, index) {
    if (currentMatch && currentMatch.length > 2) {
      throw new Error(`Unsupported token: ${currentMatch}`);
    }
    const showLeadingZeros2 = currentMatch ? currentMatch.length === 2 : false;
    return (0, import_jsx_runtime39.jsx)(MinuteInput, { ...commonInputProps, ...commonTimeInputProps, ariaLabel: minuteAriaLabel, autoFocus: index === 0 && autoFocus, hour, inputRef: minuteInput, placeholder: minutePlaceholder, showLeadingZeros: showLeadingZeros2, value: minute }, "minute");
  }
  function renderSecond(currentMatch, index) {
    if (currentMatch && currentMatch.length > 2) {
      throw new Error(`Unsupported token: ${currentMatch}`);
    }
    const showLeadingZeros2 = currentMatch ? currentMatch.length === 2 : true;
    return (0, import_jsx_runtime39.jsx)(SecondInput, { ...commonInputProps, ...commonTimeInputProps, ariaLabel: secondAriaLabel, autoFocus: index === 0 && autoFocus, hour, inputRef: secondInput, minute, placeholder: secondPlaceholder, showLeadingZeros: showLeadingZeros2, value: second }, "second");
  }
  function renderAmPm(_currentMatch, index) {
    return (0, import_jsx_runtime39.jsx)(AmPm, { ...commonInputProps, ...commonTimeInputProps, ariaLabel: amPmAriaLabel, autoFocus: index === 0 && autoFocus, inputRef: amPmInput, locale, onChange, value: amPm }, "ampm");
  }
  function renderCustomInputsInternal() {
    const elementFunctions = {
      d: renderDay,
      M: renderMonth,
      y: renderYear,
      h: renderHour,
      H: renderHour,
      m: renderMinute,
      s: renderSecond,
      a: renderAmPm
    };
    const allowMultipleInstances = typeof format !== "undefined";
    return renderCustomInputs(placeholder, elementFunctions, allowMultipleInstances);
  }
  function renderNativeInput() {
    return (0, import_jsx_runtime39.jsx)(NativeInput, { ariaLabel: nativeInputAriaLabel, disabled, maxDate: maxDate || defaultMaxDate2, minDate: minDate || defaultMinDate2, name, onChange: onChangeNative, required, value, valueType }, "datetime");
  }
  return (0, import_jsx_runtime39.jsxs)("div", { className: className8, onClick, children: [renderNativeInput(), renderCustomInputsInternal()] });
}

// node_modules/react-datetime-picker/dist/DateTimePicker.js
var baseClassName2 = "react-datetime-picker";
var outsideActionEvents = ["mousedown", "focusin", "touchstart"];
var allViews3 = ["hour", "minute", "second"];
var iconProps = {
  xmlns: "http://www.w3.org/2000/svg",
  width: 19,
  height: 19,
  viewBox: "0 0 19 19",
  stroke: "black",
  strokeWidth: 2
};
var CalendarIcon = (0, import_jsx_runtime40.jsxs)("svg", { ...iconProps, "aria-hidden": "true", className: `${baseClassName2}__calendar-button__icon ${baseClassName2}__button__icon`, children: [(0, import_jsx_runtime40.jsx)("rect", { fill: "none", height: "15", width: "15", x: "2", y: "2" }), (0, import_jsx_runtime40.jsx)("line", { x1: "6", x2: "6", y1: "0", y2: "4" }), (0, import_jsx_runtime40.jsx)("line", { x1: "13", x2: "13", y1: "0", y2: "4" })] });
var ClearIcon = (0, import_jsx_runtime40.jsxs)("svg", { ...iconProps, "aria-hidden": "true", className: `${baseClassName2}__clear-button__icon ${baseClassName2}__button__icon`, children: [(0, import_jsx_runtime40.jsx)("line", { x1: "4", x2: "15", y1: "4", y2: "15" }), (0, import_jsx_runtime40.jsx)("line", { x1: "15", x2: "4", y1: "4", y2: "15" })] });
function DateTimePicker(props) {
  const { amPmAriaLabel, autoFocus, calendarAriaLabel, calendarIcon = CalendarIcon, className: className8, clearAriaLabel, clearIcon = ClearIcon, closeWidgets: shouldCloseWidgetsOnSelect = true, "data-testid": dataTestid, dayAriaLabel, dayPlaceholder, disableCalendar, disableClock, disabled, format, hourAriaLabel, hourPlaceholder, id, isCalendarOpen: isCalendarOpenProps = null, isClockOpen: isClockOpenProps = null, locale, maxDate, maxDetail = "minute", minDate, minuteAriaLabel, minutePlaceholder, monthAriaLabel, monthPlaceholder, name = "datetime", nativeInputAriaLabel, onCalendarClose, onCalendarOpen, onChange: onChangeProps, onClockClose, onClockOpen, onFocus: onFocusProps, onInvalidChange, openWidgetsOnFocus = true, required, secondAriaLabel, secondPlaceholder, shouldCloseWidgets, shouldOpenWidgets, showLeadingZeros, value, yearAriaLabel, yearPlaceholder, ...otherProps } = props;
  const [isCalendarOpen, setIsCalendarOpen] = (0, import_react10.useState)(isCalendarOpenProps);
  const [isClockOpen, setIsClockOpen] = (0, import_react10.useState)(isClockOpenProps);
  const wrapper = (0, import_react10.useRef)(null);
  const calendarWrapper = (0, import_react10.useRef)(null);
  const clockWrapper = (0, import_react10.useRef)(null);
  (0, import_react10.useEffect)(() => {
    setIsCalendarOpen(isCalendarOpenProps);
  }, [isCalendarOpenProps]);
  (0, import_react10.useEffect)(() => {
    setIsClockOpen(isClockOpenProps);
  }, [isClockOpenProps]);
  function openCalendar({ reason }) {
    if (shouldOpenWidgets) {
      if (!shouldOpenWidgets({ reason, widget: "calendar" })) {
        return;
      }
    }
    setIsClockOpen(isClockOpen ? false : isClockOpen);
    setIsCalendarOpen(true);
    if (onCalendarOpen) {
      onCalendarOpen();
    }
  }
  const closeCalendar = (0, import_react10.useCallback)(({ reason }) => {
    if (shouldCloseWidgets) {
      if (!shouldCloseWidgets({ reason, widget: "calendar" })) {
        return;
      }
    }
    setIsCalendarOpen(false);
    if (onCalendarClose) {
      onCalendarClose();
    }
  }, [onCalendarClose, shouldCloseWidgets]);
  function toggleCalendar() {
    if (isCalendarOpen) {
      closeCalendar({ reason: "buttonClick" });
    } else {
      openCalendar({ reason: "buttonClick" });
    }
  }
  function openClock({ reason }) {
    if (shouldOpenWidgets) {
      if (!shouldOpenWidgets({ reason, widget: "clock" })) {
        return;
      }
    }
    setIsCalendarOpen(isCalendarOpen ? false : isCalendarOpen);
    setIsClockOpen(true);
    if (onClockOpen) {
      onClockOpen();
    }
  }
  const closeClock = (0, import_react10.useCallback)(({ reason }) => {
    if (shouldCloseWidgets) {
      if (!shouldCloseWidgets({ reason, widget: "clock" })) {
        return;
      }
    }
    setIsClockOpen(false);
    if (onClockClose) {
      onClockClose();
    }
  }, [onClockClose, shouldCloseWidgets]);
  const closeWidgets = (0, import_react10.useCallback)(({ reason }) => {
    closeCalendar({ reason });
    closeClock({ reason });
  }, [closeCalendar, closeClock]);
  function onChange(value2, shouldCloseWidgets2 = shouldCloseWidgetsOnSelect) {
    if (shouldCloseWidgets2) {
      closeWidgets({ reason: "select" });
    }
    if (onChangeProps) {
      onChangeProps(value2);
    }
  }
  function onDateChange(nextValue, shouldCloseWidgets2) {
    const [nextValueFrom] = Array.isArray(nextValue) ? nextValue : [nextValue];
    const [valueFrom] = Array.isArray(value) ? value : [value];
    if (valueFrom && nextValueFrom) {
      const valueFromDate = new Date(valueFrom);
      const nextValueFromWithHour = new Date(nextValueFrom);
      nextValueFromWithHour.setHours(valueFromDate.getHours(), valueFromDate.getMinutes(), valueFromDate.getSeconds(), valueFromDate.getMilliseconds());
      onChange(nextValueFromWithHour, shouldCloseWidgets2);
    } else {
      onChange(nextValueFrom, shouldCloseWidgets2);
    }
  }
  function onFocus3(event) {
    if (onFocusProps) {
      onFocusProps(event);
    }
    if (
      // Internet Explorer still fires onFocus on disabled elements
      disabled || !openWidgetsOnFocus || event.target.dataset.select === "true"
    ) {
      return;
    }
    switch (event.target.name) {
      case "day":
      case "month":
      case "year": {
        if (isCalendarOpen) {
          return;
        }
        openCalendar({ reason: "focus" });
        break;
      }
      case "hour12":
      case "hour24":
      case "minute":
      case "second": {
        if (isClockOpen) {
          return;
        }
        openClock({ reason: "focus" });
        break;
      }
      default:
    }
  }
  const onKeyDown = (0, import_react10.useCallback)((event) => {
    if (event.key === "Escape") {
      closeWidgets({ reason: "escape" });
    }
  }, [closeWidgets]);
  function clear() {
    onChange(null);
  }
  function stopPropagation(event) {
    event.stopPropagation();
  }
  const onOutsideAction = (0, import_react10.useCallback)((event) => {
    const { current: wrapperEl } = wrapper;
    const { current: calendarWrapperEl } = calendarWrapper;
    const { current: clockWrapperEl } = clockWrapper;
    const target = "composedPath" in event ? event.composedPath()[0] : event.target;
    if (target && wrapperEl && !wrapperEl.contains(target) && (!calendarWrapperEl || !calendarWrapperEl.contains(target)) && (!clockWrapperEl || !clockWrapperEl.contains(target))) {
      closeWidgets({ reason: "outsideAction" });
    }
  }, [closeWidgets]);
  const handleOutsideActionListeners = (0, import_react10.useCallback)((shouldListen = isCalendarOpen || isClockOpen) => {
    for (const event of outsideActionEvents) {
      if (shouldListen) {
        document.addEventListener(event, onOutsideAction);
      } else {
        document.removeEventListener(event, onOutsideAction);
      }
    }
    if (shouldListen) {
      document.addEventListener("keydown", onKeyDown);
    } else {
      document.removeEventListener("keydown", onKeyDown);
    }
  }, [isCalendarOpen, isClockOpen, onOutsideAction, onKeyDown]);
  (0, import_react10.useEffect)(() => {
    handleOutsideActionListeners();
    return () => {
      handleOutsideActionListeners(false);
    };
  }, [handleOutsideActionListeners]);
  function renderInputs() {
    const [valueFrom] = Array.isArray(value) ? value : [value];
    const ariaLabelProps = {
      amPmAriaLabel,
      dayAriaLabel,
      hourAriaLabel,
      minuteAriaLabel,
      monthAriaLabel,
      nativeInputAriaLabel,
      secondAriaLabel,
      yearAriaLabel
    };
    const placeholderProps = {
      dayPlaceholder,
      hourPlaceholder,
      minutePlaceholder,
      monthPlaceholder,
      secondPlaceholder,
      yearPlaceholder
    };
    return (0, import_jsx_runtime40.jsxs)("div", { className: `${baseClassName2}__wrapper`, children: [(0, import_jsx_runtime40.jsx)(DateTimeInput, { ...ariaLabelProps, ...placeholderProps, autoFocus, className: `${baseClassName2}__inputGroup`, disabled, format, isWidgetOpen: isCalendarOpen || isClockOpen, locale, maxDate, maxDetail, minDate, name, onChange, onInvalidChange, required, showLeadingZeros, value: valueFrom }), clearIcon !== null && (0, import_jsx_runtime40.jsx)("button", { "aria-label": clearAriaLabel, className: `${baseClassName2}__clear-button ${baseClassName2}__button`, "data-testid": "clear-button", disabled, onClick: clear, onFocus: stopPropagation, type: "button", children: typeof clearIcon === "function" ? (0, import_react10.createElement)(clearIcon) : clearIcon }), calendarIcon !== null && !disableCalendar && (0, import_jsx_runtime40.jsx)("button", { "aria-expanded": isCalendarOpen || false, "aria-label": calendarAriaLabel, className: `${baseClassName2}__calendar-button ${baseClassName2}__button`, "data-testid": "calendar-button", disabled, onClick: toggleCalendar, onFocus: stopPropagation, type: "button", children: typeof calendarIcon === "function" ? (0, import_react10.createElement)(calendarIcon) : calendarIcon })] });
  }
  function renderCalendar() {
    if (isCalendarOpen === null || disableCalendar) {
      return null;
    }
    const { calendarProps, portalContainer, value: value2 } = props;
    const className9 = `${baseClassName2}__calendar`;
    const classNames = clsx_default(className9, `${className9}--${isCalendarOpen ? "open" : "closed"}`);
    const calendar = (0, import_jsx_runtime40.jsx)(dist_default2, { locale, maxDate, minDate, onChange: (value3) => onDateChange(value3), value: value2, ...calendarProps });
    return portalContainer ? (0, import_react_dom.createPortal)((0, import_jsx_runtime40.jsx)("div", { ref: calendarWrapper, className: classNames, children: calendar }), portalContainer) : (0, import_jsx_runtime40.jsx)(dist_default4, { children: (0, import_jsx_runtime40.jsx)("div", { ref: (ref) => {
      if (ref && !isCalendarOpen) {
        ref.removeAttribute("style");
      }
    }, className: classNames, children: calendar }) });
  }
  function renderClock() {
    if (isClockOpen === null || disableClock) {
      return null;
    }
    const { clockProps, maxDetail: maxDetail2 = "minute", portalContainer, value: value2 } = props;
    const className9 = `${baseClassName2}__clock`;
    const classNames = clsx_default(className9, `${className9}--${isClockOpen ? "open" : "closed"}`);
    const [valueFrom] = Array.isArray(value2) ? value2 : [value2];
    const maxDetailIndex = allViews3.indexOf(maxDetail2);
    const clock = (0, import_jsx_runtime40.jsx)(dist_default3, { locale, renderMinuteHand: maxDetailIndex > 0, renderSecondHand: maxDetailIndex > 1, value: valueFrom, ...clockProps });
    return portalContainer ? (0, import_react_dom.createPortal)((0, import_jsx_runtime40.jsx)("div", { ref: clockWrapper, className: classNames, children: clock }), portalContainer) : (0, import_jsx_runtime40.jsx)(dist_default4, { children: (0, import_jsx_runtime40.jsx)("div", { ref: (ref) => {
      if (ref && !isClockOpen) {
        ref.removeAttribute("style");
      }
    }, className: classNames, children: clock }) });
  }
  const eventProps = (0, import_react10.useMemo)(
    () => makeEventProps(otherProps),
    // biome-ignore lint/correctness/useExhaustiveDependencies: FIXME
    [otherProps]
  );
  return (0, import_jsx_runtime40.jsxs)("div", { className: clsx_default(baseClassName2, `${baseClassName2}--${isCalendarOpen || isClockOpen ? "open" : "closed"}`, `${baseClassName2}--${disabled ? "disabled" : "enabled"}`, className8), "data-testid": dataTestid, id, ...eventProps, onFocus: onFocus3, ref: wrapper, children: [renderInputs(), renderCalendar(), renderClock()] });
}

// node_modules/react-datetime-picker/dist/index.js
var dist_default5 = DateTimePicker;
export {
  DateTimePicker,
  dist_default5 as default
};
//# sourceMappingURL=react-datetime-picker.js.map
